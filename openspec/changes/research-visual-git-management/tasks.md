## 1. 临时验证基础与仓库适配

- [x] 1.1 在 `plugins/llmwiki-lite/tests/test_git_web.py` 建立真实临时仓库 fixture：空仓库、分叉/双父历史、UU冲突、未选已暂存文件、两项目、bare远端及第二个clone；所有源文件和Git配置均隔离到临时目录，不读写真实科研仓库。验证：fixture可重复创建/清理，记录已有Git测试基线；临时提交身份不写global；不覆盖第一份在途修改。（D-01、D-10）
- [ ] 1.2 新增薄适配 `scripts/git_web.py`，按注册项目解析仓库、capabilities、HEAD和正在进行的操作；实现根目录绑定、空仓库/游离HEAD分流、unsupported只读及state_root约束，复用既有注册和Git模块。验证：A-01、A-02、A-03真实仓库用例通过；父仓库不被改绑，不创建仓库、迁移项目或改.gitignore。（G-01、D-03）
- [x] 1.3 在既有命令封装中补齐非交互环境、读取禁optional locks、Windows隐藏进程及30/90秒超时；明确阻止无法保证无窗口的自定义执行器，不禁用用户hook/签名策略。验证：参数测试证明无shell/外部diff/textconv/自动editor，缺认证不打开窗口；仅打开页面/预览前后HEAD、索引、配置与文件不变；真实Windows观测留到8.2。（G-14、A-31、D-08）

## 2. 真实状态、图与确认契约

- [x] 2.1 按D-04接入status/graph读取，保留现有page语义，补齐远端引用、真实父连线、分页边界和refs快照；不为展示计数自动联网。验证：A-06、A-07通过；超过200提交的分叉图追加时泳道连续、双父边正确，移动非当前分支也使旧快照失效。（G-03、D-05）
- [ ] 2.2 接入commit/diff读取和稳定file_id，分别实现历史节点、保存完整文件和恢复影响的差异；完整返回轻量文件元数据，正文按需读取并按1MiB/5000行截断。验证：A-08、A-09通过；首提交/第一父基准、增删重命名、中文/空格/换行/前导短横线路径正确；恢复差异比较H→T，不混用工作区；101个变更文件不会漏选；HTML内容不执行。（G-04、G-05、G-09、D-04、D-05）
- [ ] 2.3 按D-04、D-06实现preview/execute白名单、5分钟一次性预览、内容/索引/refs复核、按common-dir共享锁和错误码；所有写路由复用同源保护。验证：A-29、A-30通过；同modified再次修改、预览期间变化、重复确认、双标签写入、过期/重启、伪造引用与跨项目file_id均不越权或重复写入；超时不假报成功。（G-13）

## 3. 保存、身份与分支

- [x] 3.1 在 `git_operations.py` 增加完整文件选择提交能力并接入save动作，不再直接提交整个索引；保存说明与file_ids按预览校验。验证：A-10、A-11、A-12及A-03首提交通过；用Git树和索引断言仅选中内容进入提交、未选暂存项原样保留，新增/删除/重命名准确；注入提交失败后文件和输入不丢、实际暂存状态可见，远端未改变。（G-05、D-06）
- [x] 3.2 接入仓库本地作者身份读取/显式设置；提交类操作在改文件前检查身份，不自动猜姓名邮箱，不自动重放失败操作。验证：A-33通过；空全局配置下网页可填写后重新保存，config写入仅限当前仓库，缺身份时恢复操作尚未修改文件。（G-15、D-04、D-06）
- [x] 3.3 接入从节点创建分支及显式切换；名称用Git自身规则，创建不切换，切换须clean且无外部在途操作/覆盖风险。验证：A-13、A-14通过；从旧节点创建后HEAD/工作区不变，用户再点切换才改变当前分支；有已暂存、未暂存或非忽略未跟踪文件时拒绝切换、不stash、不强制覆盖。（G-06）

## 4. 合并与有限冲突处理

- [x] 4.1 复用 `git_merge.py` 接入本地分支merge，固定当前分支为目标，明确处理no_change、快进及分叉普通合并，不继承rebase/no-ff偏好改变语义。验证：A-15、A-16通过；真实parent/tree符合结果，源分支保留，dirty时拒绝，合并不触发上传。（G-07、D-06）
- [ ] 4.2 接入网站持有的合并状态、resolve/complete接口与文件revision；仅支持D-07的UU UTF-8文本冲突，整侧选择和手动最终文本均须明确标记解决。验证：A-17、A-18刷新恢复、A-19不支持/外部操作通过；残留标记、超大内容和外部索引变更阻止完成，重启仍能识别本网站操作，未确认的编辑不被当成已解决。（G-08）
- [x] 4.3 接入明确确认后的abort，复用merge --abort；复核外部改动，不使用reset兜底，不删除额外未跟踪/忽略文件。验证：A-18取消及A-19通过；取消回到原HEAD/索引/受跟踪树，网站外进行的合并无取消按钮；外部修改阻止取消并如实提示。（G-08、D-07）

## 5. 保留历史的版本恢复

- [x] 5.1 接入restore预览和执行，展示完整增删改影响及差异，生成目标树的新提交；保持当前分支和原历史，不开放reset/revert/游离checkout。验证：A-20、A-21通过；用真实Git断言R唯一父为H、R.tree=T.tree、H仍可达；同树不生成空提交，忽略/未跟踪覆盖风险和dirty状态被阻止。（G-09、D-06）
- [x] 5.2 补齐恢复后的部分成功反馈，不把restore成功但commit失败包装成操作完成；允许用户后续从保存入口完成版本。验证：A-22故障注入通过；HEAD未谎报变化、恢复后的文件保留、网页明确“文件已恢复，但版本尚未保存”，不自动回滚或上传。（G-09）

## 6. 明确目标的远端同步

- [ ] 6.1 接入已有远端/upstream读取、目标选择及显式fetch检查；限制tracking refspec，禁用自动tags/prune/子模块递归，缓存时刻真实可查。验证：A-23、A-24、A-25检查阶段通过；多远端无upstream先选目标、无远端本地功能正常，检查/取消不改HEAD/索引/工作区，含用户信息的地址及stderr脱敏。（G-10、G-11、D-08）
- [x] 6.2 接入pull_apply，固定刚检查的目标OID，再确认后复用快进/普通合并和冲突处理。验证：A-25、A-26通过；临时bare远端落后/分叉/失败均有真实结果，fetch不等于已更新代码，缓存变化拒绝旧确认，不调用可能rebase的git pull。（G-11）
- [x] 6.3 接入单分支普通push及成功后的首次upstream设置；API不接收force，错误中不出现强推建议。验证：A-27、A-28通过；确认前远端不变，成功仅指定分支更新，未提交文件/其他分支/标签不上传；非快进、缺认证、回执不明不自动重试、不抢先记成功或改upstream。（G-12）

## 7. 已确认页面与真实操作连接

- [x] 7.1 在既有 `/code` 页接入 `static/code.css`、`code.js` 和白名单静态路由，复用共享layout/ui_icon，将导航改为“代码”；按已批准图交付工具条、修改条、SVG图及可收起详情，不复制示意数据或设计选择器。验证：A-04、A-05、A-06、A-08通过；默认选HEAD、节点点击只读、旧请求不串项目，320/580/1024px无全页溢出，浅/深色和键盘可用。（G-01、G-02、D-02）
- [ ] 7.2 连接保存/身份/建分支/切换/合并/恢复弹窗与上述真实API，保留输入并在失效时要求重新确认；接入专用冲突区、逐文件确认/完成/取消，不常驻堆叠表单。验证：浏览器分别完成A-10、A-13、A-14、A-17、A-18、A-20、A-22、A-29、A-33，刷新后保留已提交的冲突解决结果；失败不跳终端、不显示假成功。（D-02、D-07）
- [ ] 7.3 连接拉取/上传目标和二次确认，显示真实缓存提示/计数，统一就地反馈、忙碌态、焦点刷新及reduced-motion；不设置后台联网轮询、不调用模型。验证：浏览器A-23至A-28、A-32、A-34通过；尚无upstream不编数字，表单/冲突草稿不被刷新覆盖，操作不改Todo、正式报告或知识，其他项目仍可阅读。（G-10至G-14、G-16、D-09）

## 8. 真实验收、回归与交接

- [x] 8.1 新增 `tests/code_browser_test.cjs`、`tests/run_code_browser.py`：后者用独立临时注册/状态目录启动既有网页、创建fixture并运行浏览器测试，结束后仅清理自己创建的临时目录。验证：执行 `python -B plugins/llmwiki-lite/tests/run_code_browser.py` 完成A-35的网页主流程和A-36临时远端流程；fixture之后的保存/切分支/合并/恢复由点击产生，脚本只读核验真实Git结果；附结果与截图位置，不用mock成功代替。（G-17、D-10）
- [ ] 8.2 在真实Windows桌面按A-31观察保存/合并/恢复与缺认证情况；按D-09基准测量5次首屏和忙碌反馈；运行同源、越界、请求大小、HTML文本注入和异常脱敏测试。验证：不弹终端/编辑器/认证窗口；首屏中位数<=2秒、忙碌态<=100ms；A-29、A-30、A-31、A-32均有结果和证据，不能仅凭subprocess mock或静态截图宣称通过。（G-13、G-14）
- [ ] 8.3 运行既有Git测试、新增适配测试、Git图验收测试、smoke和既有网站/笔记/进度回归；已落地的报告/知识/文献测试也须通过，尚未落地项明确标未验证而非假通过。验证：记录实际命令及结果，至少包含 `python -m unittest discover -s plugins/llmwiki-lite/tests -p "test_git*.py"`、`test_acceptance_git_graph.py`、`python -B plugins/llmwiki-lite/tests/smoke_test.py` 和插件AGENTS要求的校验；新增失败须修复或明确阻塞，不能删用例/覆盖其他agent改动。（A-36、D-01、D-10）
- [x] 8.4 同步插件AGENTS/README/网页Skill中的显式Git操作窄边界与使用说明，保持宿主中立；在本tasks相应项记录验收证据并更新唯一交接导航，不另写第二套规格。验证：`openspec validate research-visual-git-management --strict`通过，A-01至A-36均能定位测试/观测结果；未通过项不勾选，真实GitHub/市场发布/缓存安装/真实数据测试未获授权则仍未执行，不把本地bare结果说成GitHub已验证。（G-16、G-17、D-10）

## 9. 整站原型接入验收（已接入，专项未齐）

- [ ] 9.1 在第二份共享壳与本份真实 Git 主流程完成后，对照整站 HTML 接入代码区域、完整分支 SVG、跨栏目/项目切换和深浅色/窄屏。验证：A-37，保留原 A-35/A-36 的临时仓库真实断言与无窗口证据；不重复开发全站导航，不用内存模拟补假成功。


## 10. 2026-09-20 已读内容即时交互与并行修复

- [x] 10.1 修复既有Git恢复辅助函数错误并保留安全边界；全Git179项无失败（1项跳过），smoke374项无失败（3项跳过）。
- [x] 10.2 合并首次读取的重复Git命令；按项目/OID缓存已读详情与差异、有界串行预取，不缓存写入授权；完整Git浏览器14组和缓存专项6组通过，18次已读节点实际内容绘制机会最大19.1ms。（G-19、A-38、A-39）
- [x] 10.3 缓存已访问栏目的真实DOM、恢复浏览器历史，后台复核数据；生命周期保护未保存/上传/写入/弹窗输入，离开暂停/进入恢复，BFCache后不失活；导航4组和生命周期6组通过，最终6次栏目实际内容最大17.9ms。（G-19、A-38、A-39）
- [x] 10.4 对齐批准原型的共享壳与Git主要布局，保留品牌/125%/现有手动笔记；原型11组几何样式、六栏目切换、五宽Git布局及全站10路由通过。范围以verification为准，不表示全站所有像素及旧任务全部验收。

- [x] 10.5 直接按批准 HTML 对齐代码右栏的标题/元数据/文件计数/紧凑差异/底部操作；验证三圆圈 SVG 与 pull/push。真实行数专项4项、原型22组、Git浏览器14组通过，详细证据见 verification 的2026-09-20右栏补验，不扩大其他未验任务的勾选范围。

- [x] 10.6 按本轮图2重画三圆圈代码/分支/合并图标，diff只读放大、元数据本地路径、操作紧跟diff；真实浏览器A-41通过。
- [x] 10.7 启动入口与品牌总览分离，指定默认项目及文件夹拖动/键盘排序、持久化/失败回退/缓存菜单同步；临时注册表6项及真实浏览器A-42通过，不改助手current和真实注册数据。

## 实施与验收状态说明

唯一证据索引见本change的 [verification.md](verification.md)，包括每个A-01～A-37的覆盖层级、命令、结果及未测项。本次只勾实现且所列主验证已取得证据的项；未勾不表示完全未开发：

- **1.2、2.2、2.3、4.2** 已实现并有真实仓库测试；仍保留不支持仓库组合、特殊路径/正文显示、双tab/进程重启及各类冲突专项的覆盖缺口。
- **6.1、7.2、7.3** 已接入前后端；缺多远端/无upstream、dirty切换、网络失败/不明确回执、外部焦点及科研内容不写入的部分浏览器专项。A-22恢复失败后网页重新保存已补验，不再算缺失。
- **8.2** 首屏/忙碌性能、请求安全已有证据，但真实Windows桌面连续观测未做，不能勾选。
- **8.3** 本轮已修复原10个Git错误：179项Git回归为178通过/1跳过，smoke为371通过/3跳过，站点/笔记/报告及生命周期浏览器通过；全仓仍有117项既有Ruff问题，不能写“全仓通过”。
- **9.1** 共享壳、代码区域、跨栏目/项目与深浅窄屏已验；本任务要求的全部原型/无窗口证据尚未齐，保持未勾。

未访问真实GitHub，未发布或修改插件缓存，未提交/推送；真实项目和ACTIVE调度授权保持原样。不修改第二份7.5及真实定时验收勾选。
