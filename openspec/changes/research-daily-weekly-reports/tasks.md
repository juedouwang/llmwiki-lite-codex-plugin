> 整站原型确认后的 UI 增量：新增第 7 节；既有 [x] 仅代表原阶段验收，不代表新导航或连续笔记已交付。执行顺序与共享文件写集见 design D-11；不清除旧成果、不修改其他 agent 的 verification。

## 1. 范围与测试基线

- [x] 1.1 读取 proposal、唯一 spec、design 与仓库/插件 AGENTS，记录当前工作区和在途修改；确认不实施旧总计划、不改插件缓存。验证：交付说明列出本 change 写集，第一份进度规格/任务勾选未被本次改写，旧记录原文件哈希不变（A-01、A-26）。
- [ ] 1.2 建立隔离 home/source/wiki 测试夹具，包含两项目、旧block笔记/批注/截图、助手日档、任务事件、临时Git仓库和固定clock；不引入依赖。验证：单测可重复运行且不访问真实科研目录，已有笔记/进度基线结果留存（A-01、A-02、A-26）。

## 2. 报告存储与原阶段入口（新界面迁移见第 7 节）

- [ ] 2.1 按D-02/03实现报告身份、JSON元数据/索引、纯Markdown草稿/候选/正式快照和可恢复原子写；正文revision不受后台候选/状态变化影响。验证：同周期幂等、外部修改/双标签保护、人工清空保护、中断恢复与确认重复请求单测通过（A-02、A-09、A-10、A-11、A-12）。
- [x] 2.2 按D-05接入报告HTTP路由、同源保护、分页列表和记录/日报/周报切换，旧records扫描明确排除reports；多项目周报只引用同一归档文档。验证：接口状态码/越界路径/列表不带正文/原记录未重复收录，A-01、A-02、A-26的断言通过。

## 3. 连续编辑、预览和确认

- [x] 3.1 用一个原生textarea和既有Markdown渲染器实现连续编辑/预览，按R-03固定粘贴模式，空白可直接写、无block按钮；不装CodeMirror/新框架。验证：A-03至A-05浏览器测试通过，用户原始`###`例子粘贴后默认为h3、字重至少600，回编辑原文完整；保留截图。
- [ ] 3.2 复用现有上传校验，实现光标处Ctrl+V/拖入图片、上传位置token、多图顺序、失败重试、撤销重做及上传期间继续输入；不打开文件管理器。验证：A-06/A-07与图片位置/重复/撤销浏览器断言通过，原notebook上传回归通过。
- [ ] 3.3 实现600ms单飞自动保存、输入法组合保护、局部恢复稿、真实正文冲突处理、模式切换光标/滚动恢复；后台候选通知不触发伪冲突。验证：A-08/A-09，延迟响应不吞新字、失败重开可恢复、时间仅在详情可查。
- [ ] 3.4 接入确认、修订、历史只读/恢复、候选比较/明确采用和更多菜单复制/下载；不做逐键版本库。验证：A-10至A-13，正式v1保持不变、候选不自动确认、替换前草稿可恢复、重复确认无重复版本；保存“正式版+候选”截图。

## 4. 材料与生成协议

- [ ] 4.1 按D-06实现已有记录/笔记/任务历史/只读Git材料的增量清单与来源指纹，支持明确配置且授权的既有活动库只读消费；不开发全账户采集器。验证：A-14、A-15、A-18、A-19，多项目隔离、时间归属、敏感信息过滤、缺口提示、只读Git无窗口，以及报告/自动任务不反馈成新工作。
- [ ] 4.2 实现D-07三个MCP工具和完整分页/来源绑定，人工稿与机器稿明确区分，提交时重新检查保护状态/权限；不能让前台调用模型。验证：A-12至A-15、A-18/A-19/A-24/A-25的工具测试通过；中途人工编辑后结果只能进候选，新到材料未被错误消费。
- [ ] 4.3 原样打包`docs/liu-yaning-weekly-report-template.md`到插件templates，补现有研究记录Skill的明确报告生成流程和周报结构检查；保留原Skill“仅明确要求才写研究记录”的边界。验证：模板两份UTF-8字节一致，A-16/A-17的正式优先、跨年、少事实不凑数、草稿来源/格式测试通过；生成器不默认套用模板中的示例项目。

## 5. 定时配置、补齐与低打扰

- [ ] 5.1 在既有设置中增加D-08字段、复制启用指令、待连接/已配置/最近成功状态；按宿主工具回执绑定真实任务，不手写宿主配置，不自动替用户开启。验证：A-20、选项校验/权限开关/后台runtime不干扰设置revision测试通过；缺时刻不使用22:00等暗中默认值。
- [ ] 5.2 实现D-09的自然日/周范围、到期顺序、14天日报与本周/上周补齐、每轮最多3篇、10分钟过期、30分钟失败间隔和每输入最多3次；没有通用队列/常驻服务。验证：fake clock覆盖A-13、A-15、A-22、A-23、A-25，以及周报早于当晚日报时可用原材料、暂停后拒绝迟到结果。
- [x] 5.3 将确切内置任务启停/复用流程、失败处理、来源覆盖限制加入README和受影响Skill/说明，前台显示宿主无回执而不编造运行成功。验证：A-20、无变化安静退出、失败不弹窗和真实未连接状态可复现，插件/Skill校验通过；不创建后台exec兜底。

## 6. 总体验收与交接

- [ ] 6.1 接入`reports_browser_test.cjs`到现有临时网站测试，跑新单测、旧笔记/进度/smoke/浏览器回归和ruff；注入60秒模型延迟测20次≤50KiB加载/保存p95≤500ms、粘贴预览≤1秒。验证：A-01至A-20、A-22至A-26逐项记录证据、样本和未测原因，保存三张关键截图；不把缺测试环境写成通过。
- [ ] 6.2 用户明确授权后，确认开发插件的新MCP工具已通过获准的开发配置加载（不手改缓存），用临时项目与真实Codex内置任务完成一次非手工触发的后台报告生成，检查没有终端弹窗/抢焦点，随后暂停/删除本次临时自动化。验证：A-21真实回执与网页报告对应；若无授权/工具/周期支持，保持本项未勾选并说明阻塞，不用mock或手动调用冒充成功。
- [ ] 6.3 交付A-01至A-34验收矩阵、修改文件、兼容结果与剩余限制，检查周报模板/主spec/交接入口一致；只有所有必要任务完成才标change完成。验证：A-27，不自动commit/push、不移动真实数据、连续笔记仅以第7节实际验收认定；不把全对话采集算作已交付。


## 2026-09-19 本地实施状态（历史，当前接线状态以第8节和verification末节为准）

阶段证据见 [verification.md](verification.md)。只勾选完整达到要求的任务；其余即使已有部分代码也保持未勾选。本地编辑与报告生成协议已可测试，真实宿主调度/回执绑定、完整来源增量取材及部分编辑器/性能验收仍未完成。此 change 未完成，不归档；未开启用户真实自动化。


## 共享链路接线补充状态（启用前历史）

新增源码一次性共享入口及真实官方回执核验；授权项目对话、Git材料、三阶段实际存储链路与失败恢复已在隔离数据上验证。报告→知识→文献不相互阻塞/回滚，真实官方任务已创建但PAUSED。详见verification末节。生产未配置/绑定、未真实定时触发；8766旧服务重启被策略拒绝，未绕过。涉及真实宿主生成、尚缺专项矩阵或综合旧lint的任务继续不勾选，不归档。

## 7. 已确认整站原型对齐（共享壳与文档已实施，内页总体验收待完成）

- [x] 7.1 读取并校验 D-11 原始 HTML 哈希，原样保存为本 change 的 `reference/approved-workbench.html` 以便其他执行者使用（不得改写源稿），复用原 CSS/SVG/布局实现共享壳和六栏目，保留原路由与项目注册/搜索能力；不复制假数据或宿主脚本。验证：A-28；从项目总览/切项目/旧深链导航均可用；后续模块只接入此壳，不重复重构。
- [x] 7.2 按 D-12 在原 notebook 模块接入 v1/v2 连续正文、批注、时间详情和首次实际保存时的无损兼容；复用已有上传/历史/冲突。验证：A-32/A-33，包括浏览不写、旧批注逐条、所有旧块类型、原始快照恢复、旧客户端冲突与外部修改保护；不批量动真实笔记。
- [x] 7.3 抽取小型共享文档交互供笔记和报告使用，移除图片按钮/file input/加块步骤，新建聚焦正文，保证 Ctrl+V、预览连续粘贴、标题特例、拖拽不离页、图片顺序与失败重试。验证：A-03 至 A-09、A-30 至 A-32；同时断言无 filechooser、正文持久化、撤销重做与项目隔离。
- [x] 7.4 接入独立报告列表/详情高亮、旧列表 302 兼容、项目/状态筛选和报告工具条；新文档使用工作台key，旧项目文档保留 owner/key/相对附件路径/草稿与正式快照，批注随草稿及正式版本留存。验证：A-01/A-02/A-10/A-12/A-34；旧 [x] 2.2 不是此增量通过证据。
- [ ] 7.5 各功能 UI 增量接入后，按 D-11 做真实临时网站六页视觉与交互回归，覆盖六种宽度、深浅色、键盘、保存刷新/重启、返回筛选位置和错误态；在原 verification 追加新证据而非覆盖旧结果。验证：A-28/A-29，列真实已连通与未完成项；真实自动化未验收仍保持原任务未勾选。

本节7.1–7.4的新证据见 verification.md「共享壳与连续文档 UI 验收」。7.1只完成六入口共用壳，不代表所有内页已对齐；7.5仍未完成。未据此勾选真实定时运行、报告专项性能或其他功能的 UI 任务。

## 8. 工作台多项目汇总与明确启用（本次增量）

- [x] 8.1 日报/周报改为全局周期键和工作台目录；均支持多个参与项目，移除归档项目设置，旧项目文档/深链不搬迁。验证：`test_workspace_reports.py`（9项，含周日启用不补启用前周报）和报告浏览器通过。
- [x] 8.2 独立列表/详情/API、截图附件、旧列表302、参与项目/状态筛选接入；旧服务的新JS拒绝写入旧范围配置，要求重启。验证：报告浏览器11组检查；不据此勾选第7节整站原型/批注任务。
- [x] 8.3 同轮先日报再周报，知识/文献以全部授权项目数为预算分批检查；撤权冻结来源失效，人工范围/候选保护。验证：WorkspaceReportTests + ScheduleTests（10项，含5项目多轮预算），不是实际定时运行证据。
- [x] 8.4 按用户已确认配置全部现有项目/本机对话/每天18:00日报/周五18:00周报，复用官方heartbeat启用并核验bind；记录真实配置，不将连接成功记为首次自动运行。
- [ ] 8.5 到计划时刻取得第一次非手工触发回执并核验网页草稿/知识维护/文献结果。未发生前保持未勾选；不开临时模型/系统定时器替代。

## 9. 野人工作台品牌、应用内尺寸与隔离演示（2026-09-20 用户新增）

- [x] 9.1 按 D-13 将网页品牌和页签名改为野人工作台，补当前项目标签与代码页项目面包屑；保留插件标识和原型 reference 原件。
- [x] 9.2 应用内 125% 缩放，同步响应式断点、导航 JS、画布点击坐标和 viewport 单位；打印恢复 100%；对齐周轴、选中色、文档标题和正文宽度。
- [x] 9.3 在主仓库外创建并注册独立演示代码/Git/wiki/state，提供明确标记的笔记、知识与任务；不加入真实采集或报告范围，不自动提交主仓库。
- [ ] 9.4 用户重启现有 8766 源码服务后确认新品牌及尺寸实际加载。当前进程仍缓存旧页面，停止/另起服务被执行策略拒绝；不通过其他入口绕过。

专项回归与隔离证据见 verification.md 末节。此增量不代替 7.5 的整站全部交互/错误态验收，也不代替 8.5 的真实定时首跑。


## 10. 正常开发对话漏采修复与当日日报补记（2026-09-20）

- [x] 10.1 删除调度/共享来源层的整线程排除；按真实自动轮次过滤并持久化增量状态，补混合线程、旧游标、注入和真实共享来源回归（A-19b）。
- [x] 10.2 支持经明确授权的唯一会话关联与历史起点；不扩及同缓存目录其他会话，来源重验/冻结授权保持一致，补跨目录及撤权测试（A-19c）。
- [x] 10.3 仅关联当前用户指定的工作台会话并完整补采2026-09-20取材范围；经乐观锁补充原工作台日报草稿，保留其他项目/批注/正式历史，重新读取核验并记录证据。不得据此勾选7.5、8.5或9.4。
