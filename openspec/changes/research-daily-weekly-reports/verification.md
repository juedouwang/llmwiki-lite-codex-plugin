# 日报周报：本地阶段交付与验收

日期：2026-09-19。状态：**实施中，不是完整验收通过，不归档**。本文件只记录代码和实测结果，不替代 proposal/spec/design，不豁免任何未完成要求。

## 本轮落地

- 科研记录内增加记录/日报/周报切换，旧笔记维持原样。连续 Markdown 编辑/预览、自动保存、截图光标插入、用户确认正式版、版本历史和候选比较已能在源码网站使用。
- 新模块 `scripts/research_reports.py`；新前端 `static/reports.js`、`static/reports.css`；模板 `templates/weekly-report.md` 与 docs 原件字节一致。
- 共享接入点：`research_records.py`（排除报告反馈）、`research_web_ui.py`、`web_server.py`、`mcp_server.py`。报告三工具为 plan/sources/finish；模型由宿主提供，前台不调用模型。
- 本轮变更还包括报告单测/浏览器 harness、smoke 入口，以及插件 README、既有研究记录/Web Skills、交接索引和本 change 状态。没有改第一份进度实现/规格；其五份规划/验收文件及 research_progress.py 共六项与实施前哈希一致，见 implementation-baseline.json。
- 现有记录/笔记/任务历史可取材，日报正式优先供周报引用。来源分页全读校验、同项目来源绑定、暂停/过期拒绝、输入重试上限、人工内容转候选均有本地机制。报告提交收据与正文/元数据用同一 redo journal 恢复，run 文件落盘失败后可重试而不多写候选。

## 真实测试结果

1. `python -B -m unittest discover -s plugins/llmwiki-lite/tests -p test_reports.py -q`：18 项通过。隔离临时 home/source/wiki，无真实科研数据。8 项生成协议测试中的 automation_id 为夹具假值，**不是宿主回执**。
2. `python -B plugins/llmwiki-lite/tests/smoke_test.py`：196 项，194 通过、2 跳过；总回归约119秒。跳过为既有符号链接环境限制；不是全仓库每个历史测试均通过的声明。
3. 新 `run_reports_browser.py`：本机 Chrome headless + 既有 Playwright，九组交互通过：入口、MD标题/粗体/表格/代码、延迟图片上传期间继续输入、正式历史、候选保护、粘贴撤销重做、双标签真冲突、窄屏、默认关闭设置。
4. 旧 `run_notebook_browser.py`：站点、进度、笔记浏览器回归通过；旧任务只读访问后字节未变。未重跑真实切换标签页30秒测试；第一份原有证据保持不变。
5. ruff 针对 research_reports.py、mcp_server.py、test_reports.py、run_reports_browser.py 通过；reports.js 语法检查通过。未宣称全仓库历史 lint 已绿。
6. 三张截图存于 `%TEMP%/llmwiki-reports-evidence/`：01-markdown-paste.png、02-image-position.png、03-formal-candidate.png。图片为有效1×1 PNG夹具，验证位置而非科研图像内容；剪贴板为浏览器合成事件，不冒充系统级真实截图粘贴。

## A-01～A-27 验收矩阵

“部分”表示已有可运行机制，但完整场景或所有约束未测/未做，不可视作通过。

| 场景 | 结果 | 实测依据 / 尚缺 |
|---|---|---|
| A-01 原入口 | 通过 | 三标签、新旧笔记及旧站点回归 |
| A-02 周期/项目 | 通过 | 同周期幂等、单一归档多项目引用、日报限本项目单测 |
| A-03 连续写作 | 通过 | 空白 textarea、无 block 按钮、连续正文交互 |
| A-04 用户标题 | 通过 | 原始 h3 粘贴预览、字重≥600、原文切回；截图01 |
| A-05 Markdown | 通过 | 粗体/表格/代码、显式源码模式不强制跳预览 |
| A-06 图片位置 | 部分 | 合成粘贴插指定光标、无文件选择器、预览可见；尚未独立做系统剪贴板与刷新后图片断言 |
| A-07 上传失败 | 部分 | 延迟上传期间继续输入与禁止确认已测；失败重试/多图/拖入需补专门场景 |
| A-08 自动保存 | 部分 | 保存状态和600ms定时已实现；中文输入法真实组合过程未测 |
| A-09 防丢冲突 | 部分 | 双标签409且不吞稿、外部文件保护已测；报告离线恢复/滚动恢复尚需完整浏览器用例 |
| A-10 正式历史 | 通过 | v1/v2、修订、正式快照不变、浏览器查看 |
| A-11 空稿/重复 | 通过 | 空白/占位token拒绝；重复确认拒绝且不产生额外版本 |
| A-12 人工保护 | 通过（本地） | 人工清空/正式版/中途人工修改结果进候选，候选不改变正文revision；截图03 |
| A-13 自动稿更新 | 部分 | 未编辑机器稿替换、无变化不重复、人工编辑转候选已测；真实宿主无人值守未接 |
| A-14 事实表达 | 部分 | 来源和Skill明确验证程度；没有真实模型输出验收，不能仅凭模拟文本认定通过 |
| A-15 无/迟到材料 | 部分 | 无材料不造空文档、新材料输入变化已测；完整迟到/午夜序列待测 |
| A-16 两项目周报 | 部分 | 两项目列表引用同一归档已测、正式日报优先已测；真实跨项目语义提炼待验 |
| A-17 少事实/跨年 | 部分 | 模板字节、结构校验、跨年identity已测；少事实不凑条数为宿主Skill约束，未验真实生成 |
| A-18 来源权限 | 部分 | disabled/disconnected不取材、不扫账户聊天；Git/events/授权活动库未接 |
| A-19 溯源反馈 | 部分 | 来源ID绑定、摘要、分页、旧records排除reports已测；增量清单、敏感路径忽略/活动库过滤仍缺 |
| A-20 配置诚实 | 部分 | 默认关闭、时间空、设置不能写runtime、显示待连接已测；真实回执绑定尚未实现 |
| A-21 真后台生成 | 未做 | 没有真实宿主任务/回执，不授权替用户启用；不得用测试假ID或手工调用替代 |
| A-22 深夜恢复 | 未完整验证 | 已按材料事件北京时间归属，待补23:40/00:10恢复序列 |
| A-23 长离线限额 | 部分 | 14日和每轮3篇逻辑已有；超窗待补齐范围提示缺失，长离线fake-clock用例待补 |
| A-24 非阻塞 | 部分 | 模型不在HTTP内、延迟上传可继续输入；60秒模型延迟+20次请求p95专项未做 |
| A-25 失败晚到 | 部分 | 三次上限/30分钟间隔/暂停/过期拒绝已测；完整并发占用和更换run竞态待补 |
| A-26 安全回归 | 部分 | 同源拒绝、2MiB、预览不执行脚本/远图、旧功能回归；更多路径/符号链接专项未完成 |
| A-27 交付证据 | 部分 | 本矩阵/截图/测试命令已给出，真实计划证据不存在，任务不能全勾 |

## 不得隐藏的剩余工作

优先继续在同一 change 内补齐，不另建旧总框架：

1. 完成 Git/events 与明确授权活动库取材、来源清单/增量缓存/忽略规则、元数据索引；目前按日期读取旧记录而非完整增量取材，不能宣称大项目扫描性能已验。
2. 实现真实宿主任务回执绑定与 runtime 新鲜度/最近执行记录，补确切启停复用流程。用户尚未选择实际运行项目/时刻；当前安装插件也尚未获准加载开发MCP工具。**6.2 在明确授权前保持未勾**。
3. 编辑器补“替换前草稿直接恢复”；现在可查看/下载，正式历史能恢复。撤销暂为自定义快照栈，尚不满足 design 中“普通键入沿用 textarea 原生 undo”的全部要求；不得以当前粘贴撤销测试通过掩盖此差异。另补150ms预览合并及报告上传失败/IME/恢复专项。
4. 生成协议补周报格式失败的失败状态记账、权限/并发/跨午夜/超窗补齐，以及60秒非阻塞性能实测。当前格式不合规会拒绝写入，宿主可修正后重交或明确提交 failed。
5. 已验证“报告生成后可通过原进度context接口写自动摘要且tasks.json不变”；这只是本地顺序调用测试。Skill规定只有可靠原始记录→任务关联时才衔接，**没有真实生成端运行，不能称‘继续上次’已经会自动更新**。

## 运行与数据边界

仅开发源码仓库 `E:/GitHub/llm-wiki-agent/next/llmwiki-lite-codex-plugin`，未 commit/push/reset、未改安装缓存、未生成用户真实科研记录。浏览器测试使用临时项目并清理自身服务；用户网站只更新代码进程，未替用户注册项目、填报告或开启计划。第一份任务不重新扩充。


## 共享链路补齐：源码联调结果（后续状态以本节为准）

### 实际交付

- 新 `research_capture_runtime.py` 只读授权项目匹配的本机会话索引和命中日志；最长目录归属、配置线程/自动任务排除、归档、增量偏移、UTF-8/半行及授权起点过滤。未授权不读正文，不以索引不可用为理由扫描全账户。单会话每轮读取有界，限量或格式变化保留 gap。
- 新 `research_sources.py` 提供已授权活动库文字、Git 提交/有界差异、路径提示。不从当前 diff 编造过去工作；事件时间与观察时间分开。对话原文落盘前脱敏，撤权后冻结报告/知识来源与迟到提交再次验证。图片内文字、其他宿主、授权前历史不在当前自动覆盖范围。
- 新 `research_schedule.py`/`research_cycle.py` 接通 `begin → 三阶段call → finish`，增加3个MCP工具（共36个）。只读官方 heartbeat 回执后绑定同一配置；一轮排他、过期恢复、阶段独立失败、实际回执汇总；取材/宿主等待在短锁之外。CLI 一次运行后退出，无系统定时器/后台模型/exec 服务。
- 设置增加显式本机对话授权/撤销；复制指令包含真实配置、源码 CLI 和 Skill 路径。使用源码避免旧安装缓存缺新MCP工具；不修改缓存。旧 Python 网页进程配新静态 JS 仍能读写原设置，连接按钮提示先重启，避免升级中空指针导致设置页失效。
- 知识和文献消费已成功检查的对话版本，排除发生在 SQL 限量之前；同 ID 修订、迟到旧消息不会永久卡在前1000条。文献 inventory/read 冷却/失败上限、租约恢复、并发finish和部分落盘重放已补。

### 本轮验证与证据类型

| 验证 | 实际结果 | 边界 |
|---|---|---|
| 最终 smoke | 322项：319通过、3项符号链接环境跳过，166.606秒 | `%TEMP%/workbench-chain-smoke-final.log`；仍不包含历史已知失败的 Git/workbench_store 套件 |
| 新来源专项 | 42项：41通过、1项环境跳过（包含在smoke） | 临时 Git/SQLite/会话；含1003条分批与同ID修订 |
| 捕获/共享调度/实际存储全链路 | 9/9/2项通过（包含在smoke） | `test_capture_runtime.py`、`test_research_schedule.py`、`test_research_workflow.py`；后者真实dispatch及三处存储，宿主回答为人工注明的测试输入，不是无人值守模型 |
| 浏览器 | 报告、知识、文献、全站/笔记/进度回归通过 | 临时home + Chrome headless；报告另测授权/撤销、新JS对旧服务兼容；修了异步候选预览测试等待，不以状态文字早于渲染冒判失败 |
| 非阻塞 | 保持宿主知识领取60.07秒，前台各20次；读取p95 36.69ms、保存41.86ms、状态6.62ms | `%TEMP%/workbench-knowledge-performance.log`，隔离的未完成领取，不是真实模型；另有慢stage期间仍能保存暂停设置的并发测试 |
| 规范检查 | 插件、7个Skill、opencode dry-run、三个change strict均通过；JS语法通过 | 未安装依赖；未发布 |
| Ruff | 本轮涉及报告/来源/采集/调度/知识/文献/MCP/测试文件通过 | 全仓仍157项既有Git/workbench_store相关错误；不能称全仓lint绿 |

### 生产状态与仍未完成

1. 已通过**官方工具真实创建** `automation`（科研工作台自动整理），heartbeat，当前 `PAUSED`，目标本线程；只读实际TOML核验过。该事实只证明计划存在，**不是运行回执**。未绑定生产runtime、未采集真实项目对话、未填写项目/时间/归档设置；`reports-settings.json` 仍不存在。
2. 首次启用仍需用户给出参与项目、日报时刻、周报星期/时刻、周报归档项目，明确对话授权；知识/文献开关不擅自替用户开启。复用现有id，官方工具ACTIVE成功后再用源码bind核验，最后等待一次实际计划触发验收。A-21/任务6.2继续未完成。
3. 本轮尝试无窗口重启8766被运行策略拒绝，未绕过；只读确认原进程仍HTTP200，仍是旧后端（无对话勾选项/源码入口字段）。因此必须重新启动源码网页服务才能使用新设置；不能把文件更新称为运行服务已更新。没有另起端口、没有终端弹窗。
4. 原矩阵中编辑器专项、完整周报跨年/迟到序列、来源缓存和其他未全场景验收仍保留部分状态。知识页真实隐藏标签专项、其他宿主和连续真实使用验收也未补造结果。本节不将三份change整体标为完成或归档。
5. 没有修改真实科研正文、安装缓存，没有commit/push。重试阶段错误不会回滚前一阶段已经保存的成果；正式/人工稿、知识旧内容仍保留确认边界。


## 2026-09-20 工作台汇总与真实计划启用（当前状态）

本节是后续增量，取代上文“当前 PAUSED、尚未授权/绑定”等旧时点连接说明；不删除历史测试证据，也不把接通计划等同于首次自动运行。

### 用户授权与真实配置

- 当前全部9个注册项目已纳入；日报每天北京时间18:00，周报每周五18:00。两者均为工作台独立的多项目单篇文档，存储在 `<home>/workspace/records/reports/`，不归档到任何项目；知识页和文献条目仍归属其来源项目。
- 真实配置：`C:/Users/lyn/AppData/Local/LLMWiki/reports-settings.json`。`report_scope=workspace`、`timezone=Asia/Shanghai`、`start_date=2026-09-20`；报告/知识/文献开关已开启。9个项目的本机对话读取同意已逐一核验，仅读授权起点以后的 Codex 文本；不会自动授权新项目或回填授权前聊天。
- 通过官方工具复用唯一 heartbeat，id=`automation`，目标为本线程 `01a0ba3e-81b3-75c0-908a-da1473bccd7b`，状态 **ACTIVE**，每天18:00触发。源码 bind 于北京时间2026-09-20 01:00:52核验官方回执，三阶段连接为 `configured`；实际计划提示词与源码 `automation_prompt()` 完全相同。没有改写官方任务配置文件来伪造连接，也没有引入后台模型、`codex exec` 或系统定时器。
- **尚无首次非手工触发的自动运行回执，不能标 A-21/真实定时验收完成。** 最早日报触发为9月20日18:00，首次周报到期为9月25日18:00。周日首次启用不会补造启用前周五的周报；18:00是触发时间，不是保证全部生成完成的时刻。
- 同轮先日报、再次 plan 后周报，再独立检查知识和文献。每轮报告预算3篇；后两阶段按已选项目数分批检查，不只消费首批3个项目。新增知识/有据追加可自动保存，旧内容修改仍待确认；文献只收录真实讨论的论文，默认不下载。
- 授权后只读会话元数据检查命中24个可关联会话，3个项目有索引与原文项目不一致的跳过提示；未猜归属、未为测试读取真实正文或填造科研报告。其他宿主、图片识别与授权前历史仍不是自动覆盖范围。

### 本轮回归与已知边界

- 本轮 smoke 运行快照 **332项：329通过、3项符号链接环境跳过，171.935秒**（`%TEMP%/workspace-all-smoke-final.log`）；含工作台报告9项、共享调度10项与实际存储链路。并行分支仍在变动，此数仅代表该次执行；smoke 明示排除的既有失败套件不据此宣称通过。
- 最新报告浏览器 **11组通过**（`%TEMP%/workspace-reports-browser-recheck.log`），含独立入口、多项目归属、Markdown/截图、正式版、候选保护、撤销重做、双页冲突、授权撤销和旧服务兼容。并行 UI 线程已将旧编辑模式选择器改为实际按钮；此前选择器失配失败不能删去或冒称当时成功。
- 知识浏览器 **7组通过**（`%TEMP%/workspace-knowledge-browser-recheck.log`）；文献浏览器 **10组通过**（`%TEMP%/workspace-literature-browser-recheck.log`）。均为临时 home/项目和测试来源，不是生产定时语义总结验收。
- 本轮相关 Python Ruff、三份 JS 语法、三份变更 Skill、插件结构、opencode dry-run、三份 change strict 校验及 `git diff --check` 通过。
- **全仓 Ruff 仍有157项错误**，集中于既有 Git/workbench_store 等其他范围，未顺手修改。最新全站/笔记组合浏览器中站点子测试通过，随后进度子测试在 `progress_browser_test.cjs:81` 报“项目列表缺少 A 的行”（旧选择器与并行界面改造不匹配）；因此不能称该组合回归全部通过，也不能据此勾选整站 UI/连续笔记任务。
- 用户现有8766进程仍是旧后端（只读设置接口未返回 `report_scope`/`config_path`）。此前停止重启被策略拒绝，未换方法绕过；**需要用户手动重启源码网页服务**才展示新入口。自动任务直接调用源码 CLI，不依赖旧网页进程或安装缓存。
- 未提交/推送、未更新安装插件、未生成测试用真实科研正文。真实自动运行验收仍未勾选；其他线程负责的 UI 任务状态保持不变。


## 2026-09-20 共享壳与连续文档 UI 验收（D-11/D-12）

这是共享界面执行线程的后续证据，追加而不删除前面的历史结果。只认定本节所列的实际覆盖；不将已启用的真实计划改回关闭，不以本地浏览器测试代替首次定时执行。

### 实施边界与写集

- 仅在开发源码仓库实施，未修改安装缓存、真实科研正文、生产设置或自动化启停状态；没有 commit/push。保留其他线程的报告调度、知识、文献、Git 在途修改。
- 已读取原始 HTML/CSS/SVG 并原样保存 `reference/approved-workbench.html`。原文件与副本 SHA-256 均为 `2abb585862148b1defbd9df3883b2cf138cdd9d692fa9bb5ce629a94762bcaaf`。没有复制原型假数据、宿主脚本或新建前端服务。
- 共用壳：`research_web_ui.py`、`static/style.css`、`static/app.js`；六栏目固定顺序、工作台报告独立入口、切项目保留栏目、完整三节点 Git 图标、本地 SVG 许可证 `static/lucide-LICENSE.txt`。
- 共用连续编辑：新增 `document_editor.py`、`static/document-editor.js`、`static/document-editor.css`；`research_notebook.py`/`static/notebook.js`、`research_reports.py`/`static/reports.js` 为各自存储适配器，不合并笔记与报告的数据归属。
- `web_server.py` 只接连续笔记读视图、UTF-8 预览限额、新静态资源与旧代码页共用壳参数；Git 业务路由和完整代码页交给 Git 执行线程，不重复重写。
- 更新插件 README、网页 Skill、笔记/站点/报告浏览器测试；新增 `test_continuous_documents.py` 并纳入 smoke。第一份 progress 测试只将项目主入口选择器改为真实 `/todos`，没有去掉任务摘要、完成排序或 MCP 断言。

### 本轮完成的行为

- 新建笔记直接聚焦连续正文，没有图片按钮、file input 或添加块。Markdown 粘贴默认预览；已有非空文档默认预览，主动编辑保持源码模式。截图按光标位置插入，预览中可连续粘贴，标题的文字/纯图片优先级明确。
- 笔记与报告共用 600ms 单飞保存、150ms 预览防抖、本机恢复稿、真实双标签冲突、引用批注、时间详情、上传重试、代码围栏及导出。时间不混入正文，未完成的上传不能确认/导出/另存。
- 旧 block 笔记只读打开或无改保存不写原件；首次实际修改才单篇转为 v2。保留所有旧类型、批注及对应内容，未知时间不编造。历史恢复通过真实 API 恢复原始字节快照，先保存恢复前版本；外部损坏文件只读/另存，旧客户端不能覆盖 v2。
- 日报/周报使用独立工作台入口，旧列表跳转、旧项目报告深链及附件归属保持。草稿/正式版本保留批注；采用候选不清空人工批注；点击“修改正式版”创建修订稿，不改 v1。
- 报告列表的项目/状态/类型/搜索条件随详情返回链接保留，并回到原列表行；只允许返回本站 `/reports`，不接受外部跳转提示。

### 证据矩阵

| 场景 | 本轮证据与结果 |
| --- | --- |
| A-01/A-02，A-28 的共用壳部分 | 站点浏览器逐一请求六入口并断言当前高亮、固定顺序、图标三圆，实际切换两个临时项目；项目首页/旧记录/知识深链、搜索、注册入口仍可用。工作台单测与报告浏览器验证独立归属及旧列表 302。此项不代表六个内页均已按原型重做。 |
| A-03 至 A-09、A-30 至 A-32 | 连续笔记浏览器 21 组检查全部通过：原始标题示例、Markdown、连续/多图截图、光标与标题优先级、上传中继续输入、失败重试、撤销重做、批注/时间、保存刷新、本机恢复、双页冲突、HTML-only 惰性取文、外部拖拽防导航、全程无 filechooser。报告浏览器复核相同共享交互及确认阻止未完成图片。 |
| A-33 | 连续文档单测覆盖全部旧块、14 条批注、无改不写、首次迁移、真实原始快照恢复、旧客户端冲突、外部修改只读、同 ID 的两个项目与附件隔离。原 notebook 回归继续通过。 |
| A-10/A-12/A-34 | 报告浏览器验证跨两项目周报只一份、项目筛选、正式版→修订稿、旧项目报告原身份与正文、批注随 v1/v2 保留、候选不自动确认、返回筛选及行锚点；单测覆盖采用/恢复后人工批注与旧正式版不变。 |
| A-29，7.5 | 仅笔记连续页面完成六宽度（1280/1024/736/580/375/320）×深浅色检查，无横向溢出且 textarea 不截断内部正文；站点旧页面桌面/窄屏与导航 Tab/Esc 回归通过。尚未完成六个内页全矩阵、真实服务重启后同一夹具恢复、完整 reduced-motion 与全部错误态视觉验收，因此不勾 7.5。 |
| A-21、报告专项性能与生产首次周期 | 本轮未执行，不据此勾选 6.1/6.2。已有真实 ACTIVE 配置保持；首次非手动实际运行仍以真实回执另验。smoke 的进度性能数字不是报告编辑器 p95 证据。 |

测试使用临时 home/source/wiki、临时 Git 仓库、真实 loopback HTTP 和已有 headless Chrome/Playwright，不访问用户科研正文。剪贴板通过测试 DataTransfer 注入，不改用户系统剪贴板；中文组合用 composition 事件模拟，不冒充真实 Windows 中文输入法人工验收。

### 最终运行快照

- 连续文档 **7 项通过**，原笔记 **20 项（19 通过、1 项符号链接环境跳过）**，报告 **18 项通过**，工作台报告 **9 项通过**。
- `run_notebook_browser.py`：站点 + 进度 + 连续笔记组合全部通过。此前“项目列表缺少 A 的行”已通过匹配真实 `/todos` 入口修复并重跑，不抹除上文原时点失败。日志：`%TEMP%/llmwiki-notebook-ui-final.log`。
- `run_reports_browser.py`：**13 组通过**，包括新增跨项目周报/历史深链、正式版批注、返回筛选位置。日志：`%TEMP%/llmwiki-reports-ui-final.log`。
- 更新后的 smoke **339 项：336 通过、3 项符号链接环境跳过，173.765 秒**。日志：`%TEMP%/llmwiki-smoke-workbench-final.log`。smoke 明示排除的既有失败套件仍未据此宣称通过。
- 本轮共用文档/笔记/报告/共用壳与相关 Python 测试的 Ruff、6 份相关 JS 语法检查、OpenSpec strict、`git diff --check` 通过。插件/Skill 校验与 opencode dry-run 前段已通过，本轮未增加运行时依赖。
- 全仓 Ruff 仍不通过：本次快照 **158 项**，包括原先其他范围的 157 项及并行 Git 实施中的 `git_web.py` 一项未定义符号，已通知对应执行者；不对别人的写集批量自动修复，也不据此声称全仓绿灯。该数字仅代表检查时点。
- 截图（均为测试数据）：`%TEMP%/llmwiki-continuous-light-1280.png`、`%TEMP%/llmwiki-continuous-dark-375.png`、`%TEMP%/llmwiki-reports-evidence/01-markdown-paste.png`、`02-image-position.png`、`03-formal-candidate.png`。已目视检查笔记桌面/窄屏深浅色及报告 Markdown 预览。

### 已修复的集成问题与剩余项

- 修复上传结束后撤销栈只替换正文、不调整选择范围导致下一图插入旧图片 URL；补合法 URL、上传中撤销及多图单事务断言。
- 修复初始化渲染期间误把另一个标签页刚写入的恢复稿当作旧恢复稿；加载期间禁止编辑，提前冻结恢复候选，双页冲突测试通过。确认期间编辑按钮禁用，避免操作被吞。
- 修复窄屏 textarea 高度未重新计算、暗色面包屑对比度及旧 details 装饰箭头；不是用截断正文掩盖溢出。
- 仅勾第 7 节 7.1–7.4。进度默认周轴/单摘要、知识目录阅读双栏、文献新增阅读交互以及完整 Git 可视化仍由各自 UI 增量完成；共享壳完成不等于整站原型全部交付。
- 现有 8766 服务仍需用户手动重启源码服务，未绕过此前停止服务的策略拒绝；未修改缓存来假装更新已安装插件。真实计划状态沿用上文 ACTIVE，首次定时运行仍未验收。

## 2026-09-20 野人工作台显示与隔离演示验收

本节对应 D-13/9.1–9.3，仅补充用户本轮品牌、125%及演示项目要求；不覆盖上文未完成项。

### 实施与数据隔离

- 网站品牌/页签改为“野人工作台”，补当前项目标签、代码页项目面包屑，收紧共享标题留白。进度默认周一开始的7天，今天用轻底色、进行中用原型紫色；连续文档正文780px、标题22px/500。未改变任务状态、自动摘要和人工内容的持久化规则。
- 应用根元素 CSS zoom=1.25，不改浏览器缩放。响应式阈值与导航JS同步，弹窗/菜单的vw/vh/dvh除倍率，旧canvas点击按实际边框换算；打印恢复1。移除Git分支选择器重复箭头。
- 注册 `project-1da5a9f6`（野人工作台 · 演示），根目录 `C:/Users/lyn/AppData/Local/LLMWiki/demos/wild-workbench-20260920`。169份代码/文档普通副本，独立source/wiki/state，无符号链接/硬链接/原仓库.git/共享objects/remote；演示git有main与demo/notes分支，DEMO.md保留一处未保存改动。主仓库没有因演示操作提交或推送。
- 两篇演示连续笔记、三页知识说明、五个任务均明确标识为演示，不构造真实科研成果；未放伪文献、未生成全局假日报周报。注册使用select=false，不切换用户当前项目。
- 复制前后逐文件SHA256验证源文件不变，copy-manifest.json记录快照。真实reports-settings.json SHA256仍为 `501d960803d682fce78004e52f44ce49cbe1f7f4137976b1214461c34635a002`；其中不含演示项目，原9项目与官方计划未修改。

### 最终专项验证

- `run_notebook_browser.py`：站点、进度、连续笔记组合通过；笔记21组交互通过，旧只读夹具字节不变。站点新增品牌/title/1.25/207.5px侧栏/7天断言，1440/1280/1024/736/580/390/375/320宽度检查；1024/580/320宽、640高时导入弹窗完整位于视口。
- `run_reports_browser.py`：13组通过。`run_code_browser.py`：全部浏览器断言通过（含320/580/1024深浅色、键盘与真实临时Git）；1000版本/1000文件首屏五次中位1270ms。此数值是Git专项，不是报告编辑器p95。
- 独立临时视觉检查：六栏目 × 七种宽度（1440/1280/1024/736/580/375/320）× 深浅色通过，另查笔记预览；无JS异常或整页横向溢出。溢出比较root.scrollWidth与root.clientWidth，不使用CSS zoom后会漏报的innerWidth。Git截图等待工作区与版本详情实际加载。
- 连续文档单测7项通过；进度18项中17通过、1项符号链接环境跳过。相关两份Python Ruff、三份JS语法检查、OpenSpec strict及git diff --check通过；未据此宣称全仓lint/smoke再次通过。
- 目视检查进度、笔记、Git桌面与窄屏深色截图。截图留在演示目录的preview/；测试日志为 `%TEMP%/llmwiki-wild-{notebook,reports,code,visual}.log`。浏览器使用隔离headless Chrome与临时loopback服务，未改真实科研正文或用户剪贴板。

### 尚未生效的常驻服务

- 只读检查：`http://127.0.0.1:8766/project/project-1da5a9f6/todos` 返回200，演示注册已可见；但8766仍由PID39716的旧源码服务持有，HTML末尾品牌仍“科研助手”，没有data-workbench-scale标记。Python页面与共享CSS缓存需要重启。
- 停止旧服务、隐藏启动另一端口曾被执行策略拒绝，未使用其他入口绕过。需要用户自行停止旧实例并重新运行源码web_server.py，不能声称常驻网页已经展示新界面。9.4因此保持未勾选。
- 原型reference哈希仍为 `2abb585862148b1defbd9df3883b2cf138cdd9d692fa9bb5ce629a94762bcaaf`。六内页逐项原型交互/错误态全验收7.5及实际定时首次运行8.5继续未勾选。

## 2026-09-20 报告入口串项目回归修复

- 根因：项目侧栏固定跳 `/reports`，列表/详情没有传入浏览项目，共享壳重读注册表默认；项目选择器又把参与项目筛选 `project` 当成浏览项目。
- 采用 D-14 的 `context` 链路，报告 owner/API 不变；补空 context 的 HTTP 解析、失效项目重选与返回链接上下文优先处理。没有写真实报告或修改默认项目/授权。
- `test_workspace_reports.py` 12项通过（新增3项导航回归，并扩充HTTP重定向/空context）；`test_reports.py` 18项通过。
- `run_reports_browser.py` 14组通过：默认B从A进入、日报/周报、参与B筛选、搜索、分页/重置页码、打开/刷新/返回、显式切B、两页隔离、新建工作台报告、旧深链原owner、显式context覆盖、失效项目重选。沿用正式历史/粘贴/冲突/设置兼容测试，没有删原断言；分页用31篇临时报告。
- `run_notebook_browser.py` 站点/进度/笔记组合通过，连续笔记21组通过、旧只读夹具不变；5份相关Python Ruff、相关JS语法、git diff --check通过。均用临时注册表与隔离headless Chrome，没有操作真实浏览器会话。
- 日志：`%TEMP%/llmwiki-nav-{unit,reports-unit,browser,notebook-browser}.log`。本轮不据此宣称全仓smoke或整个change验收完成。
- 只读确认8766仍为PID23788的旧Python源码服务：演示项目记录页返回200，但报告侧栏href仍为 `/reports`（不带context）。源码修改尚需用户停止旧服务后双击桌面快捷方式重启，再从目标项目进入；未绕过此前服务操作拒绝。
- 真实reports-settings.json SHA256仍为 `501d960803d682fce78004e52f44ce49cbe1f7f4137976b1214461c34635a002`，自动化和9项目授权未变；未更新插件缓存、提交或推送主仓库。


## 2026-09-20 工作台开发对话漏采修复与真实日报补记（A-19b/A-19c）

### 根因及实现

- 调度层曾向采集器传入 `excluded_sessions=[runtime.target_thread_id]`，共享来源读取层又按相同线程 id 排除，单改采集器仍无法让内容进入报告/知识/文献来源。
- 当前用户聊天的 session_meta.cwd 是旧插件缓存，而非工作台注册项目目录。因此取消排除之外，还需用户明确授权的唯一会话关联，不能扩为读取所有缓存目录聊天。
- 已按自动轮次过滤：持久化字节游标及自动状态，旧游标按同一文件前缀重建；注入上下文、压缩交接及异步代理通知不结束自动轮次。普通对话讨论工作台/报告路径保留；自动来源标记、实际报告文件和工具仍被排除。
- 关联记录存于该项目 state 的 workbench/capture-sessions.json，宿主/项目授权仍独立检查；共享来源按会话使用明确起点，关联变更使取材及报告冻结授权失效。未调整真实项目名单、日程或自动化，未更新插件缓存、提交或推送。

### 测试与真实验证

- 联合运行 `test_capture*.py`、`test_research_sources.py`、`test_research_schedule.py`、`test_reports.py`、`test_workspace_reports.py`、`test_report_literature_sources.py`：118项，117通过、1项环境跳过。新增真实适配器→活动库→三阶段共享来源集成断言，验证定时线程中的正常开发可取材且自动消息不入账；保留原自动标记、身份核验、项目隔离、撤权、乐观锁和来源防自循环测试。
- 8份相关Python文件 Ruff通过；本 change OpenSpec strict通过，git diff --check通过。未声称本轮全仓smoke、Git全套或浏览器全套重跑。
- 用户授权补记后，仅关联当前会话 `01a0ba3e-81b3-75c0-908a-da1473bccd7b` 到原已选 `llm-wiki-agent-55eba50f`，明确起点为北京时间2026-09-20 00:00。6批读完当时45,565,017字节的唯一日志，400条去重活动入库，读取无错误/坏行/剩余页；更早内容仅作解析跨页定位，没有入库。同缓存目录其他会话未补采。
- 真实共享来源提供器返回该线程39条可用文字来源，含当天跨项目报告配置和Git交付正常对话；18:01:28—18:47:41之前的自动任务窗口活动为0。数量包含上下文交接摘要，写日报时只把它们作为线索，未将其回顾的历史成果按摘要时间重复计入当天；未声称39条都是独立科研成果或全账户覆盖。
- 使用报告 `load/update(save)` 与实时 revision 将工作台改进补入原 `daily-2026-09-20`，没有另建逐项目日报。补记跨项目报告、Git、连续编辑/品牌/布局/演示、桌面形态讨论及本轮漏采修复，标明协作回告与本轮亲测边界。保存后重新读取：正文4958字，仍是人工编辑草稿；原相机板/其他项目/产品调研正文、批注、正式历史、原生成来源元数据均保留，真实reports-settings.json保存前后字节一致。
- 真实8766的 `/api/reports/daily/2026-09-20` 与详情页均HTTP 200，API已包含新增补记正文。没有停止/重启常驻服务或规避历史执行限制；下次定时CLI直接加载修复后的源码。

### 保留缺口

本轮仅修正常开发对话漏采并补记日报。最新真实回执中报告/文献阶段完成，知识阶段6个项目仍为SOURCE_REVIEW_INCOMPLETE；未重新运行它们或改写旧回执。7.5、8.5、9.4仍未据此勾选；其他项目取材限制和跨宿主未接入情况保持真实。
