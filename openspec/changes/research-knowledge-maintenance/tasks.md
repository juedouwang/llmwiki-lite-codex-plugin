## 1. 材料与状态基础

- [x] 1.1 新增 knowledge_maintenance 模块及 D-03 的 JSON/短锁/原子写、紧凑 proposal 索引；只初始化临时测试项目。验证旧项目没有状态文件仍正常读取，检查/实际更新时间区分，非法目标路径被拒绝；覆盖 A-19、A-20、A-27。
- [x] 1.2 实现 D-04 的独立增量基线、源码与记录入口、源输出排除和删除线索；复用原忽略/记录解析，不改 manifest 或 hooks。验证自定义 Wiki/state 路径、相同哈希、共享 snapshot 变化、自动输出、缺聊天及媒体/超限材料的覆盖提示；覆盖 A-05、A-07、A-08、A-21、A-22。
- [x] 1.3 实现冻结材料、固定批次、来源/知识目录/页面/上下文分页与已完整读取校验；完成失败不推进、首次多批和按页重检。验证20项/200000字符限制、cursor完整性、未读完不标完成、来源变化重试不丢材料；覆盖 A-06、A-11、A-12。

## 2. 落地与确认规则

- [x] 2.1 实现 create/append/replace 三动作及来源摘录校验：新建与严格末尾追加自动落地，replace 只保存建议；索引只改生成区域。验证原始字节前缀（含CRLF/尾空格）、源依据、禁止目标、AI旧文也不得直接覆盖、原 index 用户内容保留；覆盖 A-09、A-10、A-19、A-27。
- [x] 2.2 实现 accept/keep、原文/证据双版本门槛、stale与重新核实、拒绝证据键去重。验证相同依据换措辞不重提、无关文件不解禁、真实新依据可重检、保留原文不写正文；覆盖 A-11、A-12、A-15 至 A-18。
- [x] 2.3 实现逐页回执、短锁并发、重复 finish/确认幂等及单页失败恢复。用中途写入失败和两窗口提交测试证明不会重复追加、不回滚其他独立成功页、不推进失败相关源版本；覆盖 A-06、A-15、A-26。

## 3. 宿主入口与共享计划

- [x] 3.1 注册 D-07 的3个MCP工具并更新 llmwiki-maintain Skill；维护流程不再直接调用低层 Wiki 覆盖工具。通过协议测试和受控材料宿主检查，证明无价值变化可零动作完成，架构说明保留原始依据，报告推测不升级成事实；覆盖 A-01、A-02、A-07、A-10。
- [x] 3.2 在第二份报告设置中加入 knowledge_enabled 和真实绑定回执字段，默认关闭；按 D-02 扩展同一任务的调度外层，不改报告生成器职责。验证报告无到期/返回失败仍可检查知识，报告先发布，开关关闭/未绑定时知识不偷跑；覆盖 A-03、A-23、A-24。
- [x] 3.3 实现每日槽、未处理批次追赶、项目公平选择、10分钟占用、30分钟重试间隔/3次上限和对话手动路径。fake-clock 验证同日不因提交立即执行、休眠后不补造历史、手动不打开定时开关、暂停后scheduled结果不写、manual仍可用；覆盖 A-03 至 A-06、A-24。

## 4. 网页最小接入

- [x] 4.1 接入 D-09 的状态/建议列表/详情/确认 HTTP 路由，复用现有路径与同源校验；前台只读状态或处理明确确认。验证分页20条、跨项目隔离、409错误、keep与重复点击语义，不调用模型或扫描项目源目录；覆盖 A-12、A-15、A-16、A-26。
- [x] 4.2 在原知识列表/阅读页加入轻量角标、折叠详情和主动打开的原生对比 dialog；实现双栏/窄屏上下、Esc/焦点恢复、转义差异、只禁用当前提交项。浏览器验证无强制弹窗/跳页、关闭不拒绝、时间不入正文、来源可查；覆盖 A-13、A-14、A-19、A-20。
- [ ] 4.3 接入可见页有限状态刷新、stale提示和历史查看；保存角标、对比、过时不可采用三类截图。浏览器验证后台标签不轮询、无待处理不常驻刷新、新结果不夺取焦点、拒绝后刷新不复活；覆盖 A-12 至 A-18、A-22。

## 5. 集成与交付验收

- [ ] 5.1 完成知识单测、fake-clock调度测试、浏览器用例与现有测试入口接线；按 design D-10 做60秒推理延迟/20次请求性能测试。给出机器信息、p95和隔离证据；通过 notebook/progress/reports/smoke 与ruff回归，保护原数据与其他agent成果；覆盖 A-21、A-23、A-25 至 A-27。
- [x] 5.2 更新插件README及受影响维护/网页Skill，逐条汇总A-01至A-28的测试或人工证据，明确生成真实性、覆盖缺口和共享宿主限制。不发布插件、不改缓存、不自动commit/push；验证说明与真实功能一致，未验证项不勾选；覆盖 A-08、A-27、A-28。
- [ ] 5.3 仅在用户另行授权后进行一次真实共享内置计划验收：使用临时项目，更新已有测试任务而非新建知识专用任务，核对真实回执、报告/知识独立结果、关闭知识开关不影响报告和无终端弹窗；测试结束用官方工具恢复/暂停授权的测试配置。不影响用户其他任务。无授权或宿主能力就保留此项未完成，明确报告；覆盖 A-03、A-23、A-24、A-28。


## 共享链路接线补充状态

新增源码一次性共享入口及真实官方回执核验；授权项目对话、Git材料、三阶段实际存储链路与失败恢复已在隔离数据上验证。报告→知识→文献不相互阻塞/回滚，真实官方任务已创建但PAUSED。详见verification末节。生产未配置/绑定、未真实定时触发；8766旧服务重启被策略拒绝，未绕过。涉及真实宿主生成、尚缺专项矩阵或综合旧lint的任务继续不勾选，不归档。

## 6. 整站原型后的知识 UI 增量（未实施）

- [ ] 6.1 复用第二份共享壳，按 D-09/D-11 对齐目录/阅读、维护设置、查看更新、来源和窄屏布局；移除知识区重复任务摘要，保留旧深链和原建议对比。验证：A-13/A-14/A-29，空态与来源真实、打开页面不写盘/调用模型。
- [ ] 6.2 接入 D-11 同源知识正文读写和共享连续编辑行为；保留原frontmatter与人工保护，保存后刷新可查，旧建议不能覆盖。验证：A-30、路径/同源/双标签冲突及原维护测试回归；新 UI 证据与原本地阶段verification分开追加。

## 2026-09-20 生产连接状态更新

用户已明确授权现有9项目和本机会话读取。复用官方共享任务`automation`并设为ACTIVE，每天北京时间18:00；知识/文献开关及真实bind均已启用。日报/周报改为独立多项目文档，知识与文献仍各自归属原项目。当前只完成连接核验，首次真实计划回执仍未发生；涉及非手工触发的验收项保持未勾选。具体配置及限制以报告change的verification末节和docs/research-workbench-handoff.md为准。
