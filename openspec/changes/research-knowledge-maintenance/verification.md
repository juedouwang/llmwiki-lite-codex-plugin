# 第三份：知识维护本地实施验收

日期：2026-09-19。开发工作树 `task/workbench-mvp`；未提交、未发布、未改插件缓存。初始工作树快照见 `implementation-baseline.json`，保留前两份及其他在途改动。

## 交付与边界

已接入独立增量基线、冻结分页材料、3个MCP工具、create/append/replace、逐页回执与重试、原文/来源双版本检查、建议对比/采用/保留/历史。知识页与笔记、日报、文献分开；读笔记不会出现知识维护对话框节点。新增来源可直接落地，任何改写旧文均进入确认。状态/时间在折叠详情，不进入正文。

手动明确项目维护可用。每日槽/重试/公平选择/授权门槛用 fake clock 验证；真实共享内置任务仍未连接。第二份真实宿主绑定未交付，不能把网页开关或测试回执当真实启用。没有新建自动化、exec/终端循环、系统计划；没有修改真实科研内容。原始聊天仍缺失，只使用保存的讨论/笔记和允许的项目文本，详情显示覆盖缺口。

## 实际证据

- `tests/test_knowledge_maintenance.py`、`test_knowledge_schedule.py`：31项通过，涵盖核心、MCP、HTTP、fake clock；测试用临时source/home/wiki/state。
- `tests/run_knowledge_browser.py`：真实Chrome headless临时站点，7组场景通过：角标、双栏安全渲染、Esc焦点/不写、过时禁用、保留后刷新、采用、按页历史；没有真实切换前后台标签的证据。
- `%TEMP%/llmwiki-knowledge-evidence/01-pending.png`、`02-compare.png`、`03-stale.png`：角标/对比/过时截图；已查看对比截图。
- `tests/run_knowledge_performance.py`：Windows11 26200 / Python3.13.9；同一fake-host运行持续60.14秒未返回；普通约24KiB知识页，热启动读取/保存笔记/状态各20次，共60次。p95分别35.61 / 51.99 / 25.44ms，最大39.89 / 57.40 / 26.32ms。前台patch源scan为异常仍通过，run不变。证据 `%TEMP%/llmwiki-knowledge-evidence/performance.json`。这是模拟推理延迟，不是实际模型速度或无人值守验证。
- 受控宿主检查：当前宿主实际读取临时源码 `async load_frame -> await camera.read -> parse_bytes -> tuple(raw)`、旧同步架构页以及“考虑ROI、尚未实施”的原始记录。选择只提出架构替换，不写成ROI已验证、不追加矛盾结论、不复制记录流水账；生成报告文本被排除。finish得到1条pending、旧页字节未变；重复plan无新run。证据 `%TEMP%/llmwiki-knowledge-evidence/host-review-result.json`；这是一次受控语义检查，不是通用科研正确性保证。
- 全站/site、progress、notebook、reports浏览器回归已通过。过程中修复知识widget误挂到笔记阅读页的问题；文献入口测试改用新目录真实DOM，保留“未收藏PDF不出现”的断言。
- OpenSpec strict、插件manifest、7个Skill、opencode dry-run通过。修改范围ruff通过；全库ruff仍有157项错误，涉及Git/workbench等文件；未逐项核对开工基线，不能宣称全部为既有错误，也未对无关模块批量修复。

## A-01～A-28逐项证据

| 场景 | 证据/结果 |
|---|---|
| A-01 | 受控宿主调用关系检查；没有改任务或写日志流水账。 |
| A-02 | 零动作finish、重复plan测试；只更新检查时间，不造知识页/实际更新时间。 |
| A-03 | fake-clock每日槽和重复唤醒通过；真实共享任务未验。 |
| A-04 | 未启用时manual可运行；仅文件变化不会启动独立模型。 |
| A-05 | 独立基线、相同hash、共享manifest保持不变测试。 |
| A-06 | 20来源/200000字符批次、分页已读校验、失败不推进、追加写失败重试；过期intent已写部分恢复不重复追加。 |
| A-07 | 受控宿主保留ROI设想的不确定性；生成报告排除，非原始事实。 |
| A-08 | 缺聊天、媒体/不可读/超限提示；不扫描账户会话。 |
| A-09 | 新页自动写、CRLF/尾空格原始前缀保留、索引区域外不变测试。 |
| A-10 | 受控同步→异步冲突只提交replace；所有replace等待网页确认。 |
| A-11 | stale append保持人工编辑；来源未变仍按page_sources重排重检。 |
| A-12 | 页面/来源版本过时GET只读，POST拒绝；浏览器禁用采用并提示重检。 |
| A-13 | 浏览器初始无强弹窗、无跳页；没有启动终端。真实后台到达/切标签仍缺专项验收。 |
| A-14 | 双栏、差异、来源、Esc焦点恢复；关闭不改变pending。 |
| A-15 | 单建议采用及重试；两个线程同时确认结果一致。 |
| A-16 | keep只改建议元数据、正文原样；重复提交/刷新不复活。 |
| A-17 | 同证据换措辞、增加无关源不能解除拒绝键。 |
| A-18 | 来源版本变更触发重检；历史/按页筛选有浏览器证据。 |
| A-19 | 禁止记录/文献/隐藏目录/索引正文目标；越界/敏感源拒绝。 |
| A-20 | 旧项目无状态仍能读；检查时间与实际更新时间分离，正文不塞时间。 |
| A-21 | Wiki/state/报告输出不作为源码；自定义独立存储测试。 |
| A-22 | 手工改知识页不触发覆盖；GET有效过时显示、POST持久stale。 |
| A-23 | 模块职责分离、Skill规定报告先落盘且失败后继续检查；真实共享外层未验，不算完成。 |
| A-24 | 未绑定不偷跑、4天休眠不补造历史、暂停/运行过期拒绝新写；均为fake-clock。 |
| A-25 | 60秒未返回、各20次前台请求p95达标，见上。 |
| A-26 | 一页stale另一页created保留；回执分项、不推进失败相关来源。 |
| A-27 | 临时数据隔离；notebook/progress/reports浏览器回归；真实文件不seed。 |
| A-28 | 本文明确测试层次；未授权实际共享任务保持未完成。 |

## 未完成，不归档

- 3.2 / 5.3：真实任务更新及绑定回执、同一宿主报告→知识→文献端到端，需要第二份绑定能力及另行授权；本轮未执行。
- 4.3：有限轮询/visibilitychange已实现；真实切换标签的知识页专项验证尚未完成，不能拿第一份进度页证据替代。
- 5.1：专项性能与局部回归完成；全量smoke最终结果记录在下方，全库ruff非绿，综合项保持未勾。

## 主线程最终集成复核（2026-09-19）

- 全量 `smoke_test.py`：249项，247通过、2跳过，133.795秒；末尾 `LLM Wiki smoke test passed`。日志 `%TEMP%/phase34-smoke-final.log`。其中测试仓库中的临时commit不涉及开发仓库；开发HEAD仍为 `edc41d3`。
- 知识专项31项通过；主线程整合的知识/文献/报告/网页/MCP/测试文件Ruff通过。全仓157项Ruff错误尚未清理，综合门禁不冒称全绿。
- 两份OpenSpec严格校验通过。开发站点8766已从该源码无窗口重启，首页、项目页、文献、日报、知识状态接口均HTTP200；不代表真实自动化已启用。未写入真实科研正文、未发布插件、未改安装缓存。


## 共享入口后续集成

主线程新增统一 `research_schedule.py/research_cycle.py` 与官方回执核验；知识调度通过同一外层，与报告/文献独立。`knowledge_maintenance.py` 已接 canonical 授权活动库、在 sources/finish 复核撤权；已检查对话在 SQL LIMIT 前跳过，同 ID 新版本仍读取，不把撤权/不可读当删除。原文引用可再次校验，源正文不是宿主指令。

`test_research_workflow.py` 用临时真实原始会话→采集→报告→知识新建→文献收录进行 dispatch/落盘验证，并测重复无新任务、撤权不泄露、消费后跳过和修订重新入选。模型回答是人工夹具；真实模型与计划触发仍未验。最终smoke322项（319通过、3跳过），知识浏览器通过；60.07秒未完成领取期间前台20次读取/保存/状态p95分别36.69/41.86/6.62ms，run内容未改变。新外层也测慢阶段不持有设置锁，暂停仍能保存。

真实官方任务已创建但 **PAUSED**，未绑定生产配置、未选择真实项目/时刻、未读取真实对话；8766旧服务重启被策略拒绝，仍需重新启动才能用新设置。任务3.2的接线已完成，5.3真实计划验收保持未勾选；4.3真实隐藏页仍未验。详尽证据、生产状态和完整日志位置见 [报告共享链路核验](../research-daily-weekly-reports/verification.md#共享链路补齐源码联调结果后续状态以本节为准)。


## 2026-09-20 工作台汇总与真实计划启用（当前状态）

- 上文 PAUSED/未授权/未绑定是旧时点记录；用户现已授权全部9个注册项目。本机对话仅读授权起点以后的 Codex 文本，不自动扩大到新项目或其他宿主。
- 唯一官方 heartbeat `automation` 已 **ACTIVE**，每天北京时间18:00；周五同轮先日报后周报，再依次检查知识与文献。日报周报独立于科研项目，知识维护仍按来源项目维护。三阶段真实绑定已核验，本阶段开关已开启。
- **尚无首次非手工触发回执，真实定时验收任务仍未勾选。** 9月20日首个日报时刻、9月25日首个周报时刻尚未到达；连接状态不等于自动整理已经运行。
- 本阶段最新临时网站浏览器 **7组通过**（`%TEMP%/workspace-knowledge-browser-recheck.log`）。共享 smoke 本轮332项、329通过、3环境跳过；不能据此替代正式定时验收或并行 UI 专项。
- 9项目授权、独立报告存储、分批预算、24条会话元数据检查、现有8766旧后端需手动重启、全仓lint/其他UI失败等统一证据见 [报告验证末节](../research-daily-weekly-reports/verification.md)。不重复维护第二份共享状态，不修改其他线程的任务勾选。

