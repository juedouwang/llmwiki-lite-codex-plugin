## 1. 用户确认的五项增量范围

- [x] 1.1 对齐认可原型：每日待办页面、侧栏分组和日报周报图标；回归旧页面与交互。
- [x] 1.2 提供科研目标拆解 Skill 与原子、幂等排期写入，在科研进度查看每日安排。
- [x] 1.3 每日待办与科研进度同 ID 同存储；覆盖跨项目、日期、改期、临时任务和旧任务兼容。
- [x] 1.4 网页/MCP/CLI 复用相同服务；覆盖输入验证、冲突和真实 HTTP 链路。
- [x] 1.5 助手交付待验收、用户确认 done、科研记录留痕及重复调用幂等；运行回归验证。

## 验证记录（2026-09-22）

- 全量 smoke：477 项，3 跳过，其余通过；后续新增测试已加入同一 smoke 入口。
- 最终聚焦后端 / HTTP / CLI：59 项全部通过；并发写入用例重复 100 次通过。
- 笔记安全与兼容回归：25 项，1 跳过，其余通过；编辑器浏览器复测通过。
- 每日待办浏览器：同 ID、跨项目、创建/修改/删除、预计用时、日期/改期、父目标钻取、待验收/确认、交付记录链接、未保存保护、深色/窄屏通过。
- 旧网站 10 路由、日报周报、Markdown/Ctrl+V/图片/IME、导航与 Git 缓存浏览器回归通过。
- 修改 Python 的 Ruff、JavaScript 语法、Skill/插件校验与 OpenSpec strict 校验通过。
- 真实 8766 服务已静默重启；health、daily、daily API、reports、projects 均返回 200。全部写入测试使用隔离临时项目，未往真实科研项目添加测试数据。
- 未 commit/push、未发布 marketplace、未修改安装缓存；新 Skill/MCP 在源码中，当前安装插件需更新后再由宿主加载，固定 CLI 可直接使用。
