## 1. 目录基础与数据保护

- [x] 1.1 在开发仓库读取仓库/插件 AGENTS.md 和本 change 四份材料，保存现有 Git 状态及相关测试基线；确认第二份材料 helper 的交付情况，保护第一份与采集模块在途修改。验证：交付记录区分原有失败/本次失败和未接通依赖，不操作缓存、真实项目或远端；不以此任务授权启用计划。
- [x] 1.2 按 design D-02 修正 `literature_catalog.py` 的短写锁、重读/原子替换、条目级 revision 和旧结构兼容。验证：临时旧目录可读且不被浏览重写，两进程写入不丢条目，同条目变更冲突/无关条目新增不冲突；覆盖 A-01、A-02、A-11、A-27。
- [x] 1.3 按 D-03 实现 DOI/arXiv/URL 归一化与活动/移除记录一起查找的 upsert，接通来源去重、补缺和身份冲突；不按标题并条。验证：参数化覆盖编号前缀/版本/URL参数、同名不同论文、多个命中和矛盾强标识；A-07～A-10通过，错误候选不影响其他正常候选。
- [x] 1.4 实现人工字段白名单与清空保护、仅移除清单、明确重收恢复、自动不恢复；修正旧测试对 dismissed 结构/不支持方法参数的假设。验证：A-10、A-15、A-16、A-24通过，原文/笔记文件hash不变，迟到自动结果不能加回已移除条目。

## 2. 现有网页的手动闭环

- [ ] 2.1 按 D-04 改造 `literature_catalog_web.py` 的项目列表：50项分页、项目内搜索、原位添加、返回保留搜索与滚动；移除打开列表时的全目录导入扫描。验证：A-01～A-04、A-25通过，空项目不因存在PDF/截图出现收藏，列表不请求模型/外网。
- [x] 2.2 接通 D-04 的 add/update/delete HTTP 和详情原位编辑，修复路由/表单 action/create_item 参数/URL id 不一致；完成 Enter、输入法、Esc、提交反馈和409保留输入。验证：实际HTTP调用后重启读取仍在；A-05、A-06、A-11、A-12、A-15、A-16通过，同源检查覆盖所有文献写入口。
- [x] 2.3 接通详情原地址、当前项目原文、明确绑定笔记、可用对照阅读及折叠来源时间；保留显式导入并修复请求体二次读取，绝不自动扫描/迁移。验证：A-12～A-14、A-25、A-27、A-28通过；失效资料明确提示，另一个项目同名文件不可读，未选资料hash不变。

## 3. 收藏工具与增量收录

- [ ] 3.1 在 `mcp_server.py` 注册并实际实现 D-05 的 `llmwiki_literature_collect`；修改既有文献 Skill，明确仅收藏/下载/精读三种意图，保留七个Skill；用户明确下载/精读完成后通过同一工具绑定已存在原文/笔记，默认收藏不读全文。验证：A-13及MCP列表/调用可达，关闭日报仍能当次收藏并打开项目入口；A-17、A-18通过，项目不明先澄清，不发起下载或扫描全部聊天。
- [x] 3.2 新建 `literature_collection.py`，复用第二份已交付的三类来源 helper、授权/忽略/脱敏和稳定源身份，排除报告/知识/自动化自身/文件提示；不修改采集器。验证：受控来源同时含明确论文、普通网址、模糊标题与不可读对话，A-19、A-20通过；无源接口时如实标未接入，不以mock代替接通。
- [x] 3.3 实现并注册 literature plan/sources，以及 D-07 的日槽、分片分页、领取到期、离线续跑、失败次数与公平排序。验证：新增 `test_literature_schedule.py`，fake-clock覆盖当天完成后晚到材料、跨日积压、长源多片、未读完不提交、未变不推理、失败不推进、过期重试；A-23、A-24通过，不受报告14天窗口错误截断。
- [ ] 3.4 实现并注册 literature finish：校验冻结source_ids/原文摘录/所有标识、逐项upsert和跳过原因、写入时复查授权与移除、幂等回执及释放全文。验证：新增 `test_literature_collection.py` 覆盖伪造证据、跨项目来源、零论文、写一半中断重放、同回执重试与不同回执拒绝；A-07～A-10、A-15、A-19、A-24通过，不覆写人工资料。

## 4. 共享任务与低打扰状态

- [x] 4.1 严格按 design D-06 最小扩展报告设置、真实绑定状态与共享任务外层模板：默认关闭 literature_enabled，顺序报告→已接通知识→文献，前阶段无任务/返回失败仍检查后阶段。验证：A-21、A-22通过；实际代码未启用任何真实任务，设置意图不冒充绑定，模板没有另建后台/exec或改变原任务周期的分支。
- [ ] 4.2 实现 collection 状态/重试 HTTP 与折叠收录详情；展示真实覆盖缺口、收录时间与失败原因，不插入正文、不全站轮询，重试仅排入共享入口。验证：A-20、A-22、A-25、A-26通过；关闭/撤权后晚到finish不写，其他报告/知识阶段仍可用，无来源正文/敏感配置泄露。

## 5. 验收、回归与交付

- [ ] 5.1 新增 `literature_browser_test.cjs`，用临时项目实际执行只贴地址→收藏→搜索→详情→编辑→返回→移除→明确恢复，并打开原文/笔记；验证输入法、失败留输入和同条目并发提示。验证：按 D-08 保存少量列表/详情/错误态证据，标明 A-01～A-18、A-25、A-27、A-28对应结果，不只提交HTML断言。
- [x] 5.2 完成 D-08 的5000条目录性能和边界测试，记录环境、20次请求的median/p95及模型挂起时前台结果；补危险协议/脚本/路径越界/跨站/资料上限等用例。验证：median≤1秒、p95≤2秒、无前台源扫描/网络/子进程，A-29、A-30中本地确定性部分通过；失败先定位，不擅自增数据库或新依赖。
- [ ] 5.3 更新 README/受影响使用说明与 smoke，运行文献单测、浏览器测试及已有 notebook/progress/reports/knowledge 回归、插件 AGENTS.md 要求的校验和本 change 严格校验。验证：交付 A-01～A-33映射、变更文件、真实依赖状态和未完成项，说明哪些是基线失败；不重写前三份规划、不新增重复 docs 需求稿、不自动提交/发布。
- [ ] 5.4 仅在用户另行授权临时项目与真实共享计划后，通过官方工具更新同一任务并做宿主真实运行验收：实际论文与普通网址的区分、前阶段无任务仍收录、文献失败报告保留、关文献不关日报、无额外窗口。验证：A-19～A-24、A-26、A-30提供同任务id/实际回执/网页结果，按授权恢复原配置或清理临时任务；未获授权/工具不可用则本项保持未完成，不用手动调用或模拟回执代替。


## 2026-09-19 子线程核验说明

详见 verification.md。仅勾已实际验证的目录基础、生产HTTP手动操作/阅读与本地性能项目。2.1保留未完成（精确滚动位置未验）；3.1/3.2保留未完成（虽真实四MCP与saved helpers已测，不等于全源/宿主行为已验）；3.3/3.4保留未完成（读取失败预算、极窄中断统计窗口仍在）；4.x由主线程整合，真实共享任务未验；5.1保留未完成（完整浏览器场景矩阵尚有部分项）；5.3保留未完成（全仓lint失败、未完整核验共用交付）；5.4未授权，绝不以模拟代替真实运行。当前文献54项、生产浏览器、5000条/60秒模拟预算通过；本次集成smoke244项通过（跳过2）。


## 共享链路接线补充状态

新增源码一次性共享入口及真实官方回执核验；授权项目对话、Git材料、三阶段实际存储链路与失败恢复已在隔离数据上验证。报告→知识→文献不相互阻塞/回滚，真实官方任务已创建但PAUSED。详见verification末节。生产未配置/绑定、未真实定时触发；8766旧服务重启被策略拒绝，未绕过。涉及真实宿主生成、尚缺专项矩阵或综合旧lint的任务继续不勾选，不归档。

## 6. 整站原型后的文献 UI 增量（未实施）

- [ ] 6.1 按 D-04/D-09 接入原型列表/详情/添加编辑dialog、真实已读切换，复用现有项目清单和 update/revision。验证：A-03/A-05/A-06/A-31，原深链/筛选/移除恢复仍可用，自动收录不改阅读状态。
- [ ] 6.2 复用共享文档交互接入项目阅读笔记读写、首次保存显式ID关联、旧paper_file兼容、多篇选择及截图路径。验证：A-13/A-14/A-32/A-33、保存刷新/重启、首存关联失败后重试不覆盖正文；不扫描/下载原文，不另建编辑器。

## 2026-09-20 生产连接状态更新

用户已明确授权现有9项目和本机会话读取。复用官方共享任务`automation`并设为ACTIVE，每天北京时间18:00；知识/文献开关及真实bind均已启用。日报/周报改为独立多项目文档，知识与文献仍各自归属原项目。当前只完成连接核验，首次真实计划回执仍未发生；涉及非手工触发的验收项保持未勾选。具体配置及限制以报告change的verification末节和docs/research-workbench-handoff.md为准。
