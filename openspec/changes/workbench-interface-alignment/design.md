## Context
此前只对齐局部而保留旧正文结构。本轮直接读取批准HTML，以真实页面截图和DOM/CSS测量验收，不修改原型文件。
## Goals / Non-Goals
直接转写原型布局和共享控件；落实删除和主题。保留品牌、125%、连续编辑器、项目排序、Git保护及报告确认；不新增依赖/账号/远程服务。
## Decisions
共享rw-list/rw-list-row/rw-list-main/rw-list-meta转写原型；记录日期放标题下，搜索默认收起，主按钮共享plus SVG。知识库真实目录/Markdown分栏，仅纳入知识维护范围的页面，排除记录/文献/索引与隐藏文件；文献真实列表。“继续上次”的服务端首屏移至科研进度页，保留原任务上下文及无脚本可读入口，不在知识库重复展示；进度和报告功能不删。
进度直接转写原型“继续上次”间距、周轴和完成圆圈；四状态保留在不遮挡标题的行内菜单/任务详情，14/28天收进范围入口。报告采用左侧类型切换、右侧即时筛选和真实草稿/正式标识。知识页顶部操作连接现有维护设置/真实候选列表；首页为项目级候选，深链为该页候选。正文保持只读，不为还原示意按钮绕过已有 Wiki 写入边界。
删除/撤销POST复用请求校验与笔记锁，revision匹配；仅32位手动笔记ID，归档.notebook-trash，撤销不覆盖现有文件。附件和历史保留。
theme.js在head同步读取本地外观，设置html color-scheme；侧栏菜单与设置页事件委托支持缓存导航，原型light-dark颜色扩展到所有模块。
## Risks / Trade-offs
125%视觉尺度保留，与截图比较须统一尺度。只操作隔离测试数据。源码验收不代表常驻服务已更新；重启独立核实。
## Validation
记录行和主按钮原型几何；取消/删除/撤销/冲突/跨源；三主题、刷新、系统切换、缓存导航、窄屏；真实截图；现有编辑器、报告、Git与导航回归。
