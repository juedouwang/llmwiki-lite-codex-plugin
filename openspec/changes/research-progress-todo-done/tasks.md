## 1. 任务数据与字段归属

- [x] 1.1 在 research_progress.py 的现有任务读写中补 completed_at、context_mode 与历史快照；兼容旧 version 1，禁止只读迁移和客户端伪造完成时间。验证：test_progress.py 覆盖 A-02、A-04、A-05、A-14，旧文件读取前后字节一致。
- [x] 1.2 实现人工/自动有效内容选择、按字段覆盖、显式空值及解除覆盖；人工 API 不可写自动字段。验证：test_progress_context.py 覆盖 A-07、A-08，自动内容到达不改变任务 revision 和人工历史。

## 2. 轻量读取与摘要接入口

- [x] 2.1 按 D-01/D-03 增加 contexts.json 确定性保存、两个 MCP 工具与 schema/dispatch；校验明确项目/任务/记录，落实幂等和独立 context revision，不添加执行器。验证：A-01、A-03、A-09、A-10 的成功、重复、乱序、非法字段、跨项目和失败不覆盖测试。
- [x] 2.2 扩展 progress HTTP 的 summary/records 读取和人工 POST 响应，保留旧接口；摘要读取不能扫描 records，自动文件故障不妨碍人工任务。验证：单测将记录扫描替换为会报错的桩，summary/首页仍成功；records 分支及既有同源/冲突回归通过。

## 3. 页面与交互

- [x] 3.1 在现有项目列表、项目首页及进度页应用 P-04 的数量、排序、入口与 Done 展示；完成时间只在任务信息查看，不新增看板或重做笔记。验证：A-04 至 A-06 的隔离浏览器场景，桌面/窄屏无页面横向溢出、无嵌套链接。
- [x] 3.2 在既有任务详情补人工模式、折叠自动内容及来源、“使用自动内容”和按需笔记选项；保存失败/原记录缺失不清空用户内容。验证：A-07、A-08、A-11，普通打开/只改标题不将自动内容转成人工覆盖。
- [x] 3.3 按 D-04 补可见页的 5 秒轻量刷新、超时及单个在途限制；刷新不覆盖编辑表单或其人工 revision。验证：A-12、A-13，后台结果到达时焦点/输入不变，隐藏后不继续定时请求，无模型/终端启动。

## 4. 集成验收与交付说明

- [x] 4.1 运行 progress/context、notebook、smoke 与隔离浏览器回归；用规定样本实测 P95 并验证延迟接入不阻塞前台。验证：逐项报告 A-01 至 A-14 的测试/可复现证据，真实跑不了的项明确标未验证；不能用注入摘要证明后台自动生成已实现。
- [x] 4.2 更新插件 README、受影响的 llmwiki-web/llmwiki-research-record Skill 和 research-continuity 文档，明确三类功能分工及已完成/未接通部分，不扩大记录 Skill 的隐私授权。验证：文档与实际 MCP/页面一致，ruff/相关文档格式检查通过；交付列出改动文件、验证、遗留问题，不自动提交/推送或修改用户科研数据。

## 5. 执行反馈复核（2026-09-19）

- [x] 5.1 补通用 MCP 必填参数检查，沿用 inputSchema.required；覆盖全部有必填字段的工具以及真实 stdio 错误返回；确认空项目不回退当前项目。
- [x] 5.2 补 A-03 隔离集成测试：实际编辑源文件、实际调用 Hook 子进程，检查事件结构、任务摘要及 tasks.json/contexts.json 原始字节均不被改动。
- [x] 5.3 修复笔记浏览器测试的异步载入等待；重新执行站点、进度、笔记三组浏览器回归。具体测试结果及其他模块检查问题见 docs/research-continuity.md 的复核记录。
- [x] 5.4 A-13 原生标签页切换验收：独立临时 Chrome 可见时约 5 秒轮询；切到同窗口另一标签页 30.32 秒无新请求；切回 16 ms 立即读取，随后恢复约 5 秒轮询；未保存输入/光标/焦点保留。使用 tests/run_progress_visibility.py，未覆盖 DOM 可见性、未派发模拟事件；最小化窗口未单独验证，不自动归档。

## 6. 整站原型后的进度 UI 增量（原业务已验收，以下未实施）

- [ ] 6.1 第二份共享壳接入后，按新版 P-04/D-06 对齐单张继续上次、默认周轴/周切换、顶部新建和折叠Done，移除知识阅读区重复摘要；保留四状态和旧范围能力。验证：A-06/A-15及1280/375px对照，日期不因切视图变化。
- [ ] 6.2 回归原 A-01 至 A-14 与新增 A-16，验证旧任务/ID/完成时间/人工上下文及源文件保持，保存刷新仍在；分别记录“既有业务通过”和“新视觉通过”，未接自动生成时仍显示真实状态。
