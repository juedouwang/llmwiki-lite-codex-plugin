# 验证记录（2026-09-21）

## 修改范围
- 项目管理：下拉只选项目；当前浏览项目跨栏目保持；更多/右键菜单重命名与注销，源文件/Wiki不删除。
- 科研进度：统一任务描述、可选DDL、高中低优先级、显式完成/恢复；Ctrl+V截图和旧元数据兼容。任务描述使用Markdown输入及实时预览，不宣称完整富文本。
- 笔记/日报/周报：连续Markdown、编辑态图片、自动保存、标题立即同步、失败/离页保护；保留旧正文、批注及历史。
- 代码：批准原型图标；主/linked worktree绑定当前目录操作，共同仓库锁及占用分支保护。
- 自动整理：说明计划决定何时运行、会话关联决定材料归属；没有新建、重绑或重跑用户的真实计划。

## 已通过
- 综合 `smoke_test.py`：451项，448通过、3跳过；无失败。新增项目管理、进度兼容、多工作树测试已加入默认门禁。
- `run_site_browser.py --prototype ... --alignment --refinements`：整站10路由、原型22组几何/样式、六栏目与五档Git宽度、三种外观、项目排序/默认项、缓存导航、放大diff均通过。
- 进度专属浏览器：9组，包括真实Ctrl+V、图片持久化、上传重试、409合并、旧字段保留及跨项目隔离。
- 项目管理专属浏览器：上下文、右键/键盘菜单、重命名、失败重试、安全注销、移动端坐标通过。
- 编辑器专属浏览器：实时图片、中文IME、撤销重做、上传及保存失败、离页保护、旧内容保留、日报周报确认通过。
- 笔记完整浏览器与日报周报完整浏览器分别通过：覆盖Markdown默认预览、两页冲突、离线恢复、旧版报告深链、候选保护、六档宽度及深浅色。
- 完整页面生命周期：笔记、进度、报告编辑、报告列表、文献、知识六组通过，后台响应不串页，离页停止轮询。
- Git代理定向：200项（199通过、1跳过）；原代码浏览器14组、新多工作树浏览器4组通过。测试均为临时仓库。
- 改动Python文件Ruff通过；JS语法、diff空白检查、OpenSpec strict、opencode安装器dry-run、两份受影响Skill及Plugin校验通过。

- 原进度浏览器回归 `run_notebook_browser.py --progress-only`：退出码0，无跳过；统一描述/优先级/DDL、完成恢复及时间历史、MCP自动上下文与手写保护、跨项目隔离、轮询焦点/光标、慢来源不阻塞、笔记/记录/图片链接、旧项目文件字节不变均通过。默认入口仍运行site、progress、notebook全部组。

- 最终收尾：26个变更Python文件Ruff、21个变更JS/CJS语法、Git diff空白检查通过；源码仍为 `task/workbench-mvp` / `535f540`，没有提交或切分支。

## 验证边界
- 全库Ruff仍报告117项既有问题；本轮改动Python文件未报告问题。未顺带清理无关文件。
- 自动整理设置的最近保存状态为已连接/ACTIVE，但最近运行结果含知识维护 `SOURCE_REVIEW_INCOMPLETE`，属于既有自动维护运行问题，本轮未重跑或修复。
- 真实任务、笔记、默认项目、排序、注册表业务配置未被验收测试修改，也未将真实待办自动标记Done。
- 未切源码分支、提交或push；未改安装缓存、marketplace或版本号。

## 部署阻塞
已核对旧服务来自本仓库、8766端口、Python无窗口进程。停止旧服务并静默重启的调用被本机执行策略拒绝，未绕过限制。旧进程仍在运行，不能宣称新Python代码已在8766生效；仅刷新可能混用新静态资源与旧后端。需要用户先关闭旧服务，再双击已核对指向本源码 `desktop_launcher.py` 的桌面“野人工作台”快捷方式静默启动。

## Git 图标复核修正（2026-09-21）
- 用户复核发现图标仍不同：前一版只检查两个端点，没有校验分支曲线，错误采用了从右上节点向左下节点弯曲的另一种连接路径。
- 已从批准交互原型使用的 Lucide `git-branch` 读取真实节点定义，路径应为 `M15 6a9 9 0 0 0-9 9V3`。同步修正侧栏、分支切换按钮及动态分支菜单；不修改批准原型，也不靠截图重新绘制。
- 回归新增三处 SVG 全路径、圆心/半径、视口、尺寸、线宽及端点/连接样式与原型直接比较，避免仅检查圆圈数量导致漏检。
- 修正后验收通过：整站10路由、3处SVG精确比对、22组原型几何/样式、六栏目切换及五档Git宽度。已目视对照实际页与原型的图标截图；SVG节点与样式一致，不宣称截图逐像素一致。变更Python文件Ruff、JS语法及diff检查通过。

## 剩余两项待办修正（2026-09-21）

- 对应真实待办 `d8a76a9b95fe4c56bf312d814f8a0936`（自动整理保存报错）及 `7261a88e8b4e4a16833c91aafd7b45df`（进度跨度及表单对齐）。只读核实，未改变其完成状态。
- 报错根因：历史设置仍有注销项目 ID，保存新选择后的撤销授权循环调用 `project_for` 解析旧 ID，从而抛出“Project is not registered”。修复仅跳过注销项目的旧授权清理；新提交的无效 ID 仍拒绝。未访问或删除注销项目保留文件，未改真实自动化配置。
- 时间轴默认今天起，显示今天至截止日跨度、今天截止单日条、逾期提示及跨窗口裁剪；一/二/四周和前后浏览不写任务。日期输入与优先级对齐，快捷日期位于日期字段下方，320px/480px单列不溢出。
- 先在旧实现运行新增3项设置回归，均复现失败；修复后全部通过。相关5个测试模块共85项，84通过、1跳过、无失败，20.020s；模块为 `test_reports`、`test_workspace_reports`、`test_progress`、`test_progress_context`、`test_progress_workbench`。
- 隔离浏览器11组通过：跨度/裁剪/颜色/sticky、桌面和窄屏布局、Ctrl+V、旧字段保留、显式完成恢复、冲突/离页保护、含注销项目的设置保存及刷新持久化；受保护真实项目文件未变化。截图保存在本机临时目录 `llmwiki-pending-tasks-evidence`，包含 `progress-span-light.png`、`progress-span-dark.png`、`progress-fields-aligned.png`、`progress-dialog-mobile-320.png`。
- 本轮未运行整套 smoke 或 Git 大套回归；新增设置测试已由现有 smoke 的 `WorkspaceReportTests` 门禁收录。不将先前验证结果冒充本轮全量通过。
- 部署边界不变：未停止/重启真实服务、未改安装缓存、未提交/push。需正常重启源码服务后，再在自动整理页面保存一次设置，才会清掉真实配置中的旧项目引用；不擅自启用、重绑或重跑计划。
- 收尾校验：4个本轮实现/验收 Python 文件 Ruff、2个 JS/CJS 语法、受影响 Web Skill、OpenSpec strict、diff 空白检查通过。只读检查8766仍返回旧的“截止日排期”后端模板；两个真实待办仍为 planned。

## 共享计划复用与纯色跨度条（2026-09-21）
- 只读核实已有一个 ACTIVE 的共享计划，其提示词每次读取设置文件，并未固定参与项目列表。注册新项目不会自动取得授权；勾选并保存即可被下一轮读取，无需重新连接。真实设置当前选中8个项目，未改写用户的计划、授权或任务。
- UI已连接时收起重复连接按钮；首次连接保留入口，维护入口位于折叠说明。保存参与项目明确提示下一轮自动采用；改变执行时刻提醒同步同一计划。未保存时阻止复制，连接指令不固定配置版本、项目列表或18:00。
- 去掉普通及逾期跨度条的深色左边框；保留底色、跨度、裁剪和提示，已目视检查浅色截图。
- 相关3个单测模块45项全部通过（13.664s），含新增项目保存后同一计划直接读取且官方回执不变的回归。隔离浏览器12组通过，新增已连接/首次连接/时间变更/复制前保存/无左边框检查；未自动创建真实计划。
- 本轮3个修改Python文件Ruff、2个JS/CJS语法、2份受影响Skill、OpenSpec strict与diff空白检查通过；smoke已收录新增单测，本轮未重复运行Git大套或整库smoke。
- 部署只读检查：8766已经提供新CSS，刷新可去掉边框；设置页仍是旧Python模板，需正常重启服务加载新的入口布局。没有尝试绕过此前停止服务的策略限制。
- 真实计划最近回执仍为 PARTIAL_FAILURE（日报阶段失败，部分知识来源审核未完成）；本轮不处理生成故障，不将“已连接”宣称为“自动生成成功”。

## 新项目默认参与与当日日报试跑（2026-09-21）

- 用户本轮明确更改默认行为：新注册项目自动选中，用户按需取消。源码注册入口在已有注册锁内同步选择与已保存取材宿主；重复注册/重命名不重选，首次/暂停配置不启用、不产生新计划、不扫描。注销同步移出范围和活动库配置，原文件/授权/历史报告保留。现有官方任务的提示词、时间、ID和绑定未修改。
- 修复前用临时两项目 fixture 实测：启用报告→注销一个已选项目→report_plan 抛出 `LLMWikiError: Project is not registered`，无稳定错误码时外层记为 `STAGE_FAILED`。历史 18:03 自动轮的报告阶段也是 calls=1、runs=[]、STAGE_FAILED，轮次中有当前已注销 ID；该历史回执未留具体异常文本，故只能确认复现的缺陷与症状相符，不能断言历史失败的唯一原因已证明。
- 最小修复：读取历史设置过滤注销 ID/活动库映射，不在 GET 改配置；注销时持久化清理。旧页面的 revision 不能覆盖注册/注销后的新范围；显式提交无效项目仍拒绝。测试验证未访问/修改注销项目的保留文件。
- 真实报告试跑：通过源码 report_plan / report_sources / report_finish 对 2026-09-21 取材，读取全部5页来源，由宿主撰写并保存工作台级草稿，实际引用18项，正文2046字符；run `d776a3bfc5b34c1db9785b9f782b00a5`，revision `92c14e303b7d87f5f9bcad30baeb896d97cfc53c7751097377ba827b66fded74`。报告位于 workspace/records/reports/daily-2026-09-21/draft.md；页面及API均HTTP 200，mode=draft、正式版本0。
- 取材截止北京时间19:36，缺少今天完整助手会话、部分项目活动库、超限Git内容，均在正文/来源缺口说明；旧文件与当前工作区状态未冒称今日科研完成。额外领取的9月20日补齐run用 `MANUAL_TEST_TODAY_ONLY` 结束，未改昨天正文。今天成功是手工报告协议试跑，不是官方定时触发或报告/知识/文献三阶段全成功；既有全局 PARTIAL_FAILURE 回执未伪造为成功。
- 定向6模块83项通过（26.193s）：test_reports、test_project_management、test_project_preferences、test_capture_runtime、test_workspace_reports、test_research_schedule。首次回归发现旧文案断言并同步至当前连接/默认参与语义；新增默认参与、保留取消、继承既有宿主、暂停/首次不启用、旧revision、历史注销仍能计划等场景。现有smoke门禁已加载这些类，未重复Git大套与全库smoke。
- 隔离浏览器12组通过：历史配置API不再暴露注销ID，保存清理、授权/时刻/回执保持，连接复用/首次连接/复制保护/跨主题移动端/现有笔记与进度交互不回归。使用现有runtime及已安装Chrome，无新依赖下载；首次启动因未设置本机runtime/channel失败，补足环境后通过。受保护临时项目及回执未改。
- 本轮7个Python文件Ruff、JS/CJS语法、3份受影响Skill、OpenSpec strict及diff空白校验通过。源码变更需要正常重启旧网页服务加载；未绕过此前停止服务限制，未修改安装缓存，未提交/push，未改变真实Todo完成状态。报告来源临时副本在核验后清理。
